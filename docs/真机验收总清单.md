# 真机验收总清单

> 2026-08-13 整理。背景:Phase4/Phase5 代码已完成,可离线自动化的验收项已全部由单测覆盖
> (整理时基线 cargo test 278/278 全绿,当前 349/349;每项对应的测试名见 [Phase4-手动验收清单.md](./Phase4-手动验收清单.md)
> 的三线审计结论)。**本清单只列必须真机人工验证的项**,按验证成本排序。
>
> 验证前置:装最新构建。**验收统一 release**(P3 收尾结论):`pnpm tauri build` 后装 NSIS/MSI,或 `tauri build --no-bundle` 直接跑 exe;不要用 `pnpm tauri dev`(首次编译慢会干扰验收)。Dock/抽屉/剪贴板/AI/壁纸模块**默认全开**(2026-08-14 起),自动化相关验证需先在设置中开启对应开关。

## ⭐ Phase6 UI 重构验收(2026-08-14,最高优先)

| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| P6-1 | 灵动岛三态 | 单击岛 → 展开;点 Dock 图标 → 启动+脉冲+岛收回;双击岛 → 主面板呼出;Esc 三级递进:搜索态清空退小桌面 → 非搜索态且岛展开先收岛 → 岛已收起再关面板(前两级面板保持) | 展开动画流畅(378↔648 宽);图标点击不误触发展开/拖动;启动后岛自动收回;Esc 三级逐级递进不越级(岛展开时先收岛,不直接关面板) |
| P6-2 | 岛 Dock 功能 | 悬停图标看 ✕;拖 .exe 入岛;点 ＋;运行中应用看绿点 | ✕ 只删该条;拖入自动展开+高亮+添加;绿点实时 |
| P6-3 | 主面板五视图 | 呼出面板看默认视图;点 header 右侧图标切换;打字 | 默认=小桌面(桌面文件);五视图切换即时;打字直接进入搜索态,结果在上最近打开在下 |
| P6-4 | 位置跟随 | 拖动岛到别处 → 呼出主面板;面板可见时再拖岛 | 主面板始终从岛正下方弹出并实时跟随;越界回弹不跑屏外 |
| P6-5 | 皮肤与极光缘 | 设置→主题→切 4 套皮肤+强调色 | 全部窗口(岛+主面板)即时换肤;每面板顶部极光带流动可见;拂晓下文字可读 |
| P6-6 | 热键语义 | Ctrl+Alt+D / V / A | 呼出主面板并定位到对应视图(不再弹出独立窗口) |
| P6-7 | 旧配置升级 | 用旧 config.json 启动 | API key/Dock 条目/主题不丢;浅色主题老用户自动呈现拂晓皮肤 |

**观察项**:✅ 已修复(2026-08-14,提交后见 git log):溢出阈值静态 9 → ResizeObserver 动态可视容量(按 mini-dock 实测宽度,预留 ＋/「…」按钮位),第 7~9 瓦片不再被裁剪丢失;CDP 实测 10 条→前 4 瓦片+「…」浮层全列、5 条→全显无「…」,零裁剪。若观感仍不佳(如想看更多瓦片)可再调。

## ⭐ 截图功能验收(2026-08-18,新功能)

> 全链路设计见 [docs/截图功能-设计.md](./截图功能-设计.md)。CDP 已自动验证:遮罩创建/拖选坐标换算/文件保存/剪贴板 CF_DIB/岛提示/隐藏复位/设置页热键项(开发进度 45 条)。**以下必须真机人工验证**(CDP 合成 PointerEvent 不派发真实鼠标的 pointerdown,系统级全局热键 CDP 无法驱动)。⚠️ AI 热键已让位改 Ctrl+Alt+E(截图默认 Ctrl+Alt+A,设置页可改)。

| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| S-1 | 全局热键触发 | 任意程序前台按 Ctrl+Alt+A | 全屏变暗 + 十字光标,遮罩盖住一切窗口(含置顶);再按一次不叠加 |
| S-2 | 真实鼠标拖选 | 鼠标按下拖动画出选区 | 选区实时蓝色边框 + 十字参考线 + 尺寸标注;松开**立即**截图 |
| S-3 | 剪贴板可粘贴 | 截一张图 → 打开画图/微信 → Ctrl+V | 粘贴出完整截图内容(CF_DIB,已验证系统可解码) |
| S-4 | 自动保存 | 截图后打开 `图片\Aurora 截图\` | 生成 `截图_YYYYMMDD_HHMMSS.png`,内容与选区一致 |
| S-5 | 岛提示 | 截图后看灵动岛 | 药丸内短暂提示「已截图 W×H · 已复制 · 已保存到图片」 |
| S-6 | Esc/右键取消 | 拖选前按 Esc;拖选后按 Esc;拖选后右键 | 遮罩消失,不截图、剪贴板不被改动 |
| S-7 | 误触忽略 | 在桌面上快速点一下(不拖动) | 小于 5px 视为误触,遮罩消失不截图 |
| S-8 | AI 热键让位 | 按 Ctrl+Alt+E 呼出 AI 面板 | AI 热键仍可用(已从 Ctrl+Alt+A 改到 E) |
| S-9 | 双屏跨屏 | 双屏实机:副屏拖选;跨屏拖选 | 副屏选区截图正常;跨屏拖选按 MVP 限制以起始屏为准 |
| S-10 | 热键可改 | 设置 → 截图热键录制改键 → 生效 | 新键触发截图;清空键位后截图入口注销 |

## A. 高价值快速验证(约 15 分钟,建议优先)

| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| A1 | 皮肤即时生效 | 设置 → 皮肤:切 4 套皮肤(深空极光/午夜/拂晓/翠野)、换 4 色强调色 | 全部窗口**立即**换肤(保存即生效);拂晓(浅色)皮肤下错误提示/警告文字可读(红/绿/黄不再刺眼或隐身) |
| A2 | 搜索框搜文件 | Ctrl+Shift+Space → 输入某个文件名关键词(如"发票") | 应用结果下方出现「文件」分组,文件名命中优先;↑↓ 可跨组移动,Enter 打开文件;输入不存在的词无假结果 |
| A3 | 搜索框体验 | 呼出后按 Esc(有输入时)/直接 Esc(空输入);长列表按住 ↓;空 query | 三级 Esc(先清空→岛展开先收岛→再关面板);选中项跟随滚动;空态显示最近使用列表 |
| A4 | AI 工具确认条 | AI 面板对 DeepSeek 说"打开记事本" | 模型返回工具调用后,**不直接打开**,输入区上方出现确认条「AI 请求打开:…」,点执行才打开;点取消则 AI 回复"用户已拒绝";云端模式下让 AI 读剪贴板 → 回复隐私提示且不读 |
| A5 | 剪贴板单条删除 | 复制几条文本 → 打开剪贴板面板 | 每条悬停有 ✕,点击只删该条,其余保留;全量清空仍需双击确认 |

## B. Phase4 验收(约 30 分钟)

### B1 动态壁纸 + 电池降载
| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| B1-1 | 动态壁纸注入 | 设置 → 动态壁纸开 → 选一个视频素材 | 桌面背景被壁纸窗口接管:视频在桌面图标下层播放,不遮挡图标、不抢焦点、永不出声 |
| B1-2 | 电池降载 | 笔记本拔/插电源 | 30s 内遮罩出现/消失(状态变化才切换);插电自动恢复 |
| B1-3 | 关闭恢复 | 关掉动态壁纸开关 | 桌面恢复系统默认壁纸,无残留窗口 |
| B1-4 | 目录外素材 | 选择 Pictures 之外的视频 | data URL 方案正常播放 |
| B1-5 | html 已禁用 | 尝试选 html 文件 | 素材列表不出现 html;设置失败提示正常(安全加固后 html 不再是合法素材) |

### B2 键鼠模拟自动化(设置 → 自动化开启)
| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| B2-1 | 坐标点击/文本输入 | 测试区对记事本输入(含中文) | 点击与中文输入完整送达前台窗口 |
| B2-2 | 权限边界 | 目标为管理员权限窗口 | 注入被 UIPI 拦截不生效,**不报错不崩溃**(预期行为) |
| B2-3 | 开关门控 | 总开关关后操作测试区 | 按钮失效/报错,后端不崩 |

### B3 UIA 控件操作(设置 → UIA 开关开)
| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| B3-1 | 窗口搜索/控件列表/读文本 | 测试区搜"记事本"→ 点选 → 读文本 | 窗口列表、控件多级路径(如 4.0.0)、Name/Value 正常;无值控件提示"不支持" |
| B3-2 | 点击/输入控件 | 点按钮类控件;对输入框输入 | 点击生效;SetFocus 直输或点击兜底,中文安全 |
| B3-3 | 边界文案 | 搜 UWP(系统设置)操作;对管理员窗口操作;双开关任一关操作 | 分别显示:"UWP 应用不支持第三方控件操作"/"目标窗口为管理员权限,当前程序无权限访问"/"自动化未启用,请在设置中开启" |
| B3-4 | 非法入参 | 手输非法 hwnd/控件 id | "未找到匹配的窗口/控件"或格式错误提示 |

### B4 MSI/NSIS 安装包(重项,与 B5 可合并)
| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| B4-1 | NSIS 安装 | 装 Releases 的 `Aurora_0.2.6_x64-setup.exe` | 中文向导;开始菜单/快捷方式;托盘/热键正常 |
| B4-2 | MSI 安装 | 装 `Aurora_0.2.6_x64_en-US.msi` | ARP 显示 "Aurora" + 发布者 "Aurora Desktop" + 0.2.6 + 图标 |
| B4-3 | 升级保留配置 | 装新版覆盖安装 | ARP 单条记录新版本;主题/壁纸/AI Key/热键全部保留 |
| B4-4 | 卸载 | 控制面板卸载 | Program Files\Aurora 清空、无残留进程 |

> **✅ B4-1/B4-4 已自动验证(2026-08-18,开发进度 43 条)**:NSIS /S 静默安装→%LOCALAPPDATA%\Aurora,快捷方式/ARP 正确;安装版 CDP 冒烟 12/12(URL=tauri.localhost 打包资源/config_load 真实配置/呼出/切视图/Esc/零污染);uninstall.exe /S 后目录/ARP/快捷方式/进程四清零残留;卸载不删 %APPDATA% 用户配置(B4-3 前提成立)。**仍待真机**:B4-2 MSI、B4-3 双版本覆盖升级、托盘/热键真人体验(CDP 无法驱动系统级全局热键)。

## C. Phase5 验收(约 20 分钟,部分需第二台显示器)

| # | 验证项 | 步骤 | 预期 |
|---|---|---|---|
| C1 | 自动更新-无新版本 | 设置 → 更新 → 手动检查 | 提示"已是最新"(当前 latest.json=0.2.6 与安装版一致) |
| C2 | 自动更新-全链路 | 走 GitHub 预发布流程:发布一个版本号更高的预发布 Release(上传新 NSIS 资产)→ 更新 latest.json 指向预发布版 → 设置→更新→手动检查 → 下载 → 安装;验收后把 latest.json 回滚指向正式版。⚠️ 勿用「本地静态服务器 + 改更新源 URL」路线:config_save 对 update_feed_url 有域名白名单(仅 GitHub 6 域,src-tauri/src/commands/config.rs UPDATE_FEED_HOST_WHITELIST),非 GitHub 域会被拒绝保存——确需本地服务器分发须先扩展该白名单 | 提示新版本、下载进度、SHA-256 校验通过、NSIS /S 静默安装后自动启动新版;顺手验证:手动把更新源 URL 改成非 GitHub 域 → 保存被拒并提示 |
| C3 | 多屏壁纸 | 双屏实机:开多屏开关、拼接模式、每屏素材下拉 | 逐屏独立素材生效;拼接模式跨屏连续 |
| C4 | 显示器热插拔 | 拔插显示器 | 壁纸自动重建(2s 探针),不崩溃不错位 |
| C5 | AI 换壁纸 | AI 面板说"把桌面壁纸换成 xxx" | 动态壁纸工具生效(危险工具只有 open_item 需确认,壁纸工具直接执行) |
| C6 | 深色主题观感 | 深色下打开设置 → 更新/多屏区块 | 显示正常可读 |

## D. 本轮已知限制(无需验证,知悉即可)

- 控件 id 是窗口 Z 序遍历路径,窗口重排后需重新遍历
- 降级安装(新→旧版本)可能被安装器拒绝,属预期
- 代码未签名,SmartScreen 提示"未知发布者"选"仍要运行"
- html 动态壁纸已移除(安全加固),如需网页壁纸请反馈,可评估带权限隔离的方案
