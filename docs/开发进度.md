# Aurora Desktop 开发进度

> **本文件是多 agent 协作的唯一权威进度源。**
> **规则一:任何 agent 开始任何任务前,必须先读本文件。**
> **规则二:任何任务状态变化(领取/完成/阻塞),必须立即更新本文件并提交 git。**
> **规则三:环境变化(装了什么、改了什么配置)必须记入「环境状态」。**

## 项目信息

| 项 | 值 |
|---|---|
| 项目名 | aurora-desktop(Aurora 极光) |
| 本地位置 | D:\github\show |
| 远程仓库 | ✅ https://github.com/sssst1118/aurora-desktop(2026-08-11 已推送) |
| 技术栈 | Tauri2 + Rust 1.80+ + Vue3-TS + TailwindCSS v3 + pnpm |
| 规格文档 | [docs/开发文档.md](./开发文档.md) |
| 内存目标 | 空闲 <120MB |
| 支持平台 | 仅 Windows 10/11 (最低 Win10 21H2) |

## 环境状态(新 agent 第一件事:核对这里)

| 组件 | 状态 | 说明 |
|---|---|---|
| Node.js | ✅ v24.15.0 | |
| pnpm | ✅ 11.17.0 | 2026-08-11 已装 @tauri-apps/api 等依赖;Tailwind v3.4/pinia/postcss 已装 |
| Rust / cargo | ✅ 1.97.1 | 2026-08-11 已装(C:\Users\gengzhengkun-bjb\.cargo\bin\) |
| MSVC Build Tools | ✅ 14.44.35207 | 2026-08-11 用户手动安装完成;cl.exe 在 `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\`;hello 工程 cargo build + 运行已全链路验证通过 |
| Windows SDK | ✅ 已就绪 | 随 VS Build Tools 安装;链接器验证通过 |
| git | ✅ | 本地仓库已 init(main 分支) |
| Release 构建 | ✅ | 2026-08-11 cargo build --release 99.2s 成功;exe 16.1MB;空闲内存基线 WS 37.5MB / PRIV 15MB,**达标(<120MB)** |
| Phase3 依赖 | ✅ | 2026-08-11 骨架合入时已加 Cargo.toml:reqwest 0.12(json+stream,native-tls=schannel)+ futures-util 0.3(SSE 逐行)+ tokio 1(time,SSE idle 超时用;3.1 模块编译不再缺依赖);前端无新 npm 依赖 |

## 阶段总览

| 阶段 | 内容 | 目标 | 状态 |
|---|---|---|---|
| Phase1 | MVP 最小可用版 | 高级启动器 | ✅ 完成(2026-08-12 手动验收 6/6 通过) |
| Phase2 | 效率增强 | Dock/抽屉/剪贴板/壁纸/系统状态 | ✅ 完成(2026-08-11 集成收尾;2026-08-12 手动验收全部通过:抽屉/Dock/剪贴板/系统状态/搜索框/壁纸(预览修复 c0316ee 后复测通过)) |
| Phase3 | AI 集成 | AI 对话 + function-call | ✅ 完成(2026-08-12 手动验收全部通过:DeepSeek/Ollama 对话/工具调用/文件搜索/热键/断网容错) |
| Phase4 | 高级特性 | 动态壁纸/自动化/主题/MSI | ✅ 完成(2026-08-12 集成收尾;手动验收待用户,清单见 [Phase4-手动验收清单.md](./Phase4-手动验收清单.md)) |
| Phase5 | 发布收口 | NSIS+自动更新+签名预留/多屏壁纸/AI 工具扩展 | ✅ 完成(2026-08-12 集成收尾;手动验收待用户) |
| 发布+迭代 | v0.1.0 发布与自主迭代 | GitHub Release 首发 + 三线审计修复(前端 UX/Rust 安全/文档) | ✅ 完成(2026-08-13,见下一步行动 16 条) |
| Phase6 | UI 重构(Aurora v0.2 界面) | 一岛一窗/灵动岛药丸三态/Dock 并入岛/主面板五视图/4 皮肤+极光缘 | ✅ 完成(2026-08-14 实施收尾;真机验收待用户,见下一步行动 21 条) |

状态图例:🔵 进行中 / ✅ 完成 / ⚪ 未开始 / 🟥 阻塞

## 任务看板

### Phase1 MVP(目标:可以当高级启动器跑起来)

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 1.1 | 搭建 Tauri2 项目脚手架(多窗口:island/search + 托盘) | ✅ | Task1执行agent | 2026-08-11 | 脚手架完成:双窗口(island/search)+Tailwind v3+权限白名单;cargo check 与 pnpm build 双通过(托盘归 Task2 用 Rust 代码建) |
| 1.2 | 全局热键注册,呼出/隐藏搜索框 | ✅ | Task2~6执行agent | 2026-08-11 | 默认 Ctrl+Shift+Space;含置顶闪切+前台锁绕过(Alt 伪按键+SetForegroundWindow);cargo test 14/14 通过 |
| 1.3 | 应用搜索:索引开始菜单 + 打开程序 | ✅ | Task2~6执行agent | 2026-08-11 | 递归扫用户+公共开始菜单 .lnk,大小写不敏感子串匹配 top20,mtime 增量重扫;opener 打开 |
| 1.4 | 基础设置存储(本地 json 配置) | ✅ | Task2~6执行agent | 2026-08-11 | config_save/config_load;AppConfig 全字段 #[serde(default)],老配置缺字段逐字段回退(防 Phase2 加字段丢配置,已带单测) |
| 1.5 | 灵动岛基础 UI(时间 + 简单系统状态) | ✅ | Task2~6执行agent | 2026-08-11 | island 800×48:时间 1s + CPU/内存 2s 轮询(GetSystemTimes 差商/GlobalMemoryStatusEx),毛玻璃,点击唤起搜索;focus:false 不抢焦点 |

### Phase2 效率增强

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 2.0 | 集成骨架合并 | ✅ | 集成agent | 2026-08-11 | 一次 commit:3 新窗口(dock/drawer/clipboard)+capabilities+全部依赖+AppConfig 新字段(带 #[serde(default)])+invoke_handler 占位(stubs.rs)+App.vue 分流+hotkey/托盘入口 stub;cargo check+test 14/14+pnpm build 通过;**✅ 接线完成(2026-08-11 集成收尾):invoke_handler 切真实实现+删 stubs.rs+setup 接线(剪贴板监听/抽屉 watcher)+App.vue 挂载三组件+热键/托盘动作+tray tooltip 订阅 sys-status;全量 77/77 测试+pnpm build 通过;冒烟实测:剪贴板监听端到端入库/dock 与 drawer 窗口按配置显示/内存基线已测** |
| 2.1 | Dock 栏组件 | ✅ | 模块agent(2.1) | 2026-08-12 | dock.rs 五命令(条目读写/启动聚焦/运行枚举/图标双缓存)+dock_icon.rs 子模块+15 单测(含真实 .lnk COM 往返);✅ 集成接线完成(2026-08-11):命令补 pub+invoke_handler 切换+Dock.vue 挂载 dock 窗口;✅ 2026-08-11 修复右键添加(空态无菜单/窗口裁剪两个缺陷);**✅ 2026-08-12 形态定调:独立 dock 窗口废弃,并入搜索窗口底部(用户指令,见 13 条)**:删 dock 窗口/右键菜单/定位/auto-hide;Dock.vue 重写为 SearchBar 内嵌图标排,添加=拖拽 exe/lnk 进 Dock 区,删除=悬停 ✕;dock_position/dock_auto_hide 配置废弃;全量 221/221+pnpm build 通过;✅ 2026-08-12 修复拖入 lnk 无图标(ExtractIconExW 对 .lnk 返回 0 → 先 resolve_lnk_target 再提取;曾试 SHGetFileInfoW 有并发竞态弃用),222/222+build 绿(b6be24c);✅ 2026-08-12 修复启动慢+新启动无绿点(实测根因:resolve_lnk_target 1.5s/次 + opener 开 lnk 1.3s vs 直开 exe 17ms → lnk 目标缓存+直开 exe;绿点:NoMachine 运行进程是 nxplayer.bin 而 lnk 目标是 .exe,精确路径永不匹配 → window_matches_item 扩展名不同时 stem 回退),225/225+build 绿;✅ 2026-08-12 防连点排队重复启动:后端 LAUNCHING 集合(点击未启动完 → 短路返回;后台线程轮询窗口出现/10s 超时解除,解除时机以窗口出现为准)+前端 1.5s 呼吸脉冲启动中反馈,227/227+build 绿;✅ 2026-08-12 首次打开搜索栏图标加载慢修复(实测冷启动 8 lnk 提取 1.94s,首图标付 COM 初始化 1.85s):extract_icon_pixels 改走 dock.rs 共享 LNK_TARGET_CACHE(不再独立重复解析)+预热后 8 图标 10ms(190 倍),228/228+build 绿;⚠️ 修正预热时机(第一版挂错):App.vue 是 drawer/clipboard 等窗口的根组件,**search 窗口挂 SearchBar.vue,App.vue 的预热从未执行**;且 enableDock 初始 false → Dock 懒挂载于首次呼出,提取成本仍压在"打开搜索栏之后"。改为 **SearchBar setup 即读 dock 开关**(不等 onShown),Dock 应用启动即挂载后台提取,呼出时已就绪;删除 App.vue 无效预热 |
| 2.2 | FileDrawer 桌面文件抽屉(逻辑收纳) | ✅ | 模块agent(2.2) | 2026-08-11 | classify.rs 8 类扩展名分类+drawer.rs 三命令(仅扫 FOLDERID_Desktop/上限1500/打开路径白名单)+notify 事件驱动 watcher(200ms 防抖+drawer-updated 信号)+FileDrawer/DrawerPanel.vue 前端(分类 tab/收折/空态,毛玻璃同 Island);cargo test 60/60+pnpm build 通过;✅ 集成接线完成(2026-08-11):命令补 pub+invoke_handler 切换+init_watcher 在 lib.rs setup 接线+DrawerPanel.vue 挂载 App.vue drawer 分支+热键/托盘动作已接 |
| 2.3 | 剪贴板监听 + 历史持久化 | ✅ | 模块agent-C(剪贴板) | 2026-08-11 | clipboard.rs 三命令+插件事件驱动监听(非轮询,监听与窗口解耦:setup() 即启动,惰性自启兜底)+json 持久化(%APPDATA%\com.aurora.desktop\clipboard.json 与 config 同级,上限 clipboard_max_items 默认 200,新顶旧,内容哈希去重,>64KB 拒绝)+8 单测通过;前端 ClipboardPanel.vue+stores/clipboard.ts+composables/useClipboardHistory.ts 完成(毛玻璃与 Island 一致),pnpm build 通过;✅ 集成接线完成(2026-08-11):命令补 pub+invoke_handler 切换+clipboard::setup 在 lib.rs setup 接线+ClipboardPanel.vue 挂载 clipboard 分支+热键/托盘动作已接;**冒烟实测:启动后 Set-Clipboard 文本被自动记录落盘(端到端链路通)**;回贴/搜索 UI 待 GUI 验收;**✅ 2026-08-12 手动验收通过(用户实测:复制自动记录/搜索过滤/Enter 回贴自动关窗/清空/重启持久化)** |
| 2.4 | 静态壁纸切换 | ✅ | 模块agent-D(壁纸) | 2026-08-11 | wallpaper.rs 三命令(设置/列表/当前)+WallpaperPanel.vue+9 单测完成,cargo test 23/23 与 pnpm build 通过(隔离验证,整树编译待其他模块收尾);✅ 集成接线完成(2026-08-11):invoke_handler 用 wallpaper::wallpaper_set_static_cmd / wallpaper_list_local_cmd / wallpaper_get_current_cmd(保留 _cmd+rename);WallpaperPanel.vue 挂载 Settings.vue 壁纸区块;SystemParametersInfoW 设壁纸需真窗口 GUI 验证;**⚠️ 2026-08-12 手动验收未测(用户本轮未验,待用户后续实测)**;**✅ 2026-08-12 预览修复(c0316ee):asset 协议 scope 覆盖不到自定义壁纸目录且无运行时扩展 API(用户实测 C:\ProgramData\Lenovo\Themes 预览全挂)→ 弃用 asset 预览,新增 wallpaper_thumbnail 后端缩略图命令(image 缩到 480px JPEG base64 data URI,任意目录可读),前端逐张懒加载+失败占位;cargo test 237/237+pnpm build 通过,已推送;**✅ 2026-08-12 手动验收通过(用户复测:预览可见 + 设置即换正常,与焦点抖动修复一并确认)** |
| 2.5 | 系统状态完整采集(CPU/内存/网络) | ✅ | 模块agent-E(系统状态) | 2026-08-11 | 常驻采样线程 2s 采样 CPU/内存/网络并 emit `sys-status`,island 改订阅 + 网络速率展示,sys_get_status 兼容扩展(懒启动采样线程,文件在 commands/system/system_sampler.rs,嵌套声明于 system.rs);✅ 集成接线完成(2026-08-11):tray.rs 订阅 sys-status 实时更新托盘 tooltip(CPU/内存/下载/上传);GetIfTable2 实机冒烟无崩溃,数值正确性待 GUI 观察;**✅ 2026-08-12 手动验收通过(用户实测:时间/CPU/内存/网络实时刷新,数值随负载变化合理)** |

### Phase3 AI 集成

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 3.1 | AI 对话面板 + 后端代理(DeepSeek/Ollama) | ✅ | 模块agent(3.1) | 2026-08-11 | 4 文件完成(ai/client.rs HTTP 代理双模式+SSE 流式+工具调用分片合并、commands/ai.rs 命令层+run_tool_loop 工具循环≤3轮、useAiChat.ts、AIPanel.vue);27 单测全绿(整树 142/142)+pnpm build 通过+Ollama 真实连测闭环(非流式/流式/工具调用分片均通);真实 DeepSeek 对话待用户填 key 后验收;接线(mod 声明+invoke_handler+App.vue ai_panel 分支)留集成 agent |
| 3.2 | function-call 工具调用(搜索/打开/壁纸) | ✅ | 模块agent(3.2) | 2026-08-11 | ai/tools.rs 纯函数分发器完成:6 工具白名单(tools_json OpenAI 格式)+parse_tool_calls+route(绝对路径/必填校验)+rule_match 规则兜底(仅打开/查找两类);16 单测全绿,worktree 隔离验证整树 93/93(含原 77);mod 声明待集成收尾统一加 |
| 3.3 | 自然语言文件搜索 | ✅ | 模块agent(3.3) | 2026-08-11 | commands/file_search.rs 完成:ai_search_files(async+spawn_blocking)目录白名单(⊆ai_search_roots∪桌面,越权/相对路径忽略)+ext_hint_from_query 五组映射(pdf/word/excel/图片/视频)+深度≤3(第4层截断)/每层500/结果20三上限+全局去重;12 单测全绿(worktree 隔离验证,77 原测试无回归);命令注册(mod 声明+invoke_handler)待集成 agent 接线 |

### Phase4 高级特性

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 4.0 | 骨架合并(集成 agent) | ✅ | 集成agent | 2026-08-12 | 一次 commit:wallpaper 窗口+capabilities+windows-sys 三新 feature(Win32_System_Power/UI_Accessibility/System_Variant,已 grep 0.59 源码确认)+AppConfig 11 字段(带 #[serde(default)]+2 新单测)+automation/mod.rs 空声明+tailwind darkMode:class+Settings 四区块占位+stores 同步;cargo test 144/144+pnpm build 通过;invoke_handler 占位注释待模块合入注册 |
| 4.1 | 动态壁纸(WorkerW) + 电池降载 | ✅ | agent-A(4.1)+集成agent | 2026-08-12 | ✅ 已合入 main(2026-08-12):merge feat/p4-4a+mod 声明+invoke_handler 四命令+setup 电池 watcher(双开关门控)+App.vue wallpaper 分支;cargo test 189/189(新增 23 单测)+pnpm build 通过;⚠️ WorkerW 注入算法实测修正(Win11 上 DefView 是 Progman 子窗口,FindWindowExW 找子 WorkerW 主路径+顶层兜底,见错误记录);契约增量:get_state 含 url 字段(两段式 URL 闭环);set 返回 WallpaperSetInfo{path,url:Option};真机壁纸/电池验收待 §1.6 手动验收 |
| 4.2 | 键鼠模拟自动化 | ✅ | agent-B(4.2)+集成agent | 2026-08-12 | ✅ 已合入 main(2026-08-12):merge feat/p4-4b+lib.rs 注册 6 命令+Settings 自动化测试区(坐标点击/文本输入/红字错误);cargo test 166/166(新增 22 单测)+pnpm build 通过;input_sim 五 pub 函数(click_at/type_text 为 4.3 公共契约);真机注入待 §2.5 手动验收 |
| 4.3 | UI-Automation 控件操作 | ✅ | agent-C(4.3)+集成agent | 2026-08-12 | ✅ 已合入 main(2026-08-12):merge feat/p4-4c+命令层 git mv 到 commands/uia_cmd.rs(架构:commands/ 放命令层,automation/ 只放纯封装)+lib.rs 注册 6 命令+Settings UIA 测试区(窗口搜索/控件列表/读文本/点击/输入,双开关门控)+移除接线前的 #![allow(dead_code)];cargo test 205/205(新增 16 单测)+pnpm build 通过;⚠️ 实测:UiaFind/UiaNavigate 数据通道不可用(内部格式),遍历改走 Win32 窗口树+UiaNodeFromHandle+UiaGetPropertyValue(方案 C),详见错误记录;控件 id=窗口 Z 序遍历路径(非 UIA 树序),窗口重排需重新遍历;边界文案与设计文档 §3.4 逐字一致;automation 文件 0 警告(全树仍有 6 处 Phase1/2/3 历史 dead_code 警告,与本次无关,另行立项) |
| 4.4 | 主题系统 + 模块开关完善 | ✅ | agent-D(4.4)+集成agent | 2026-08-12 | ✅ 已合入 main(2026-08-12):merge feat/p4-4d+Settings.vue 令牌迁移(78 处机械替换,含 Phase4 三区块);global.css 双 :root 令牌+theme.ts(apply_theme 三态/强调色 4 色)+main.ts bootstrap 接线+8 组件迁移;pnpm build 通过;深浅色手动验收清单见模块报告(迁移偏差:语义色/图片浮层/accent 底白字保留硬编码) |
| 4.5 | MSI 打包 | ✅ | 集成agent | 2026-08-12 | ✅ 完成(2026-08-12):bundle 元数据补齐(publisher "Aurora Desktop"/copyright/category/shortDescription/longDescription);`pnpm tauri build` 产出 `src-tauri/target/release/bundle/msi/Aurora_0.1.0_x64_en-US.msi`(6.8MB,WiX 3.14 candle→light 全链路);内存复测 WS 44.5MB/PRIV 19.5MB 达标(<120MB);安装/升级/卸载/中文路径等真机验证见 [Phase4-手动验收清单.md](./Phase4-手动验收清单.md) §4.5;⚠️ WiX 下载需 HTTPS_PROXY 环境变量(见错误记录);手动放置 WixTools 缓存无效 |

### Phase5 发布收口(设计:[Phase5-设计.md](./Phase5-设计.md))

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 5.0 | 设计/计划 | ✅ | 主导agent | 2026-08-12 | 设计文档+实施计划(781bd95/ab019ea);方案 A 三块一次交付经用户确认 |
| 5.1 | 发布链路:NSIS+自动更新+签名预留 | ✅ | 主导agent | 2026-08-12 | 三段提交:Task6 updater 实现层(5f00ccd:版本语义化比较不可解析→Equal 防误判+SHA-256+sha2 0.10.9;223/223)、Task7 命令层+接线+前端(78dcd90:update_check/download/install/open_folder+6h 定时检查+托盘菜单入口+Settings 更新区块+事件订阅;225/225)、Task8 NSIS(76a4e11:bundle.targets 加 nsis+languages SimpChinese+installMode currentUser;NSIS /S 静默=自动更新安装目标);签名=预留位(自研 updater 无签名校验,签名仅消 SmartScreen),接入指南见 [docs/代码签名接入.md](./代码签名接入.md) |
| 5.2 | 多屏壁纸 | ✅ | 主导agent | 2026-08-12 | 两段提交:Task3 枚举+坐标化注入(80f9763:MonitorInfo+layout_signature 排序签名+attach_to_workerw_at 虚拟桌面坐标+monitor_states;25/25)、Task4 命令层+热插拔(551ccf1:wallpaper_multi_monitors/multi_apply/set_monitor+setup 2s 轮询布局签名变化自动重建;217/217)、Task5 前端(fb941aa:Settings 多屏 UI+每屏素材下拉+DynamicWallpaper.vue 拼接 translate 切片/独立主屏分发+wallpaper_ 前缀窗口分流;pnpm build 过) |
| 5.3 | AI 工具扩展 | ✅ | 主导agent | 2026-08-12 | Task1 AppConfig 四字段(update_enabled/update_feed_url/wallpaper_multi_monitor/wallpaper_span_mode,#[serde(default)] 回退;b397fb0)+Task2 动态壁纸工具(5a24c38:set_dynamic_wallpaper/stop_dynamic_wallpaper,8 工具白名单,rule_match 停先判防误匹配,exec 复用 4.1 命令+开关未开失败隔离;212/212) |
| 5.4 | 收尾清理 | ✅ | 主导agent | 2026-08-12 | Task9 dead_code 全清(86a14c9:ai/client.rs connect_timeout、dock.rs pid、drawer.rs total、wallpaper_dynamic.rs span_viewport 删除,clipboard teardown 接 RunEvent::ExitRequested);cargo test 223/223+构建 0 警告 |
| 6.0 | 全功能热生效(用户要求:所有功能热启动,不重启生效) | ✅ | 主导agent | 2026-08-12 | 新增 runtime.rs 统一调度(config_save 落盘成功后 apply):热键 diff 注册/注销(抽屉/剪贴板/AI)、剪贴板 monitor start/stop(监听器仅注册一次)、抽屉 watcher 启停(managed state 保活可替换)、系统采样线程 AtomicBool 门控+stop、灵动岛窗口显隐、动态壁纸关开关即撤 WorkerW 注入;电池降载/多屏热插拔/更新检查改常驻线程每轮重读配置;前端 config_save 成功后广播 aurora:config-saved,SearchBar 监听即时刷新 Dock 开关,Settings 全量"重启后生效"文案改"保存后生效";cargo test 228/228+pnpm build 通过 |

### Phase6 UI 重构(设计:[Phase6-UI重构-设计.md](./Phase6-UI重构-设计.md),计划:[plans/2026-08-14-phase6-ui.md](./plans/2026-08-14-phase6-ui.md))

| # | 任务 | 状态 | 负责人 | 更新时间 | 备注 |
|---|---|---|---|---|---|
| 6.1 | 后端骨架:窗口收敛+岛药丸+皮肤字段+热键重定向 | ✅ | 主对话+实施agent | 2026-08-14 | 一次 commit(b517040):tauri.conf 窗口 6→2(island 378×46 药丸+search 680×520 主面板,drawer/clipboard/ai_panel 删除);AppConfig 新增 skin/island_x/island_y(#[serde(default)]);island_save_geometry 命令;show_search_window 呼出定位岛下方 12px 水平居中(越界 clamp);drawer/clipboard/AI 热键与托盘改为呼出主面板+emit panel-open-view;327/327 全绿 |
| 6.2 | 视觉系统:4 皮肤令牌包+极光缘+SVG 图标库 | ✅ | 实施agent(6.2) | 2026-08-14 | 一次 commit(15e4ba9):global.css 4 皮肤块(:root 默认 deep+data-skin=midnight/dawn/verdant,含 --aur-1/2/3 极光带三色)+.aurora-panel 玻璃类(极光缘::before 流动光带=签名元素);AuroraIcon.vue+paths.ts 13 图标(stroke 线性替代 emoji);theme.ts apply_skin+强调色新 4 色+老配置 light→dawn 兼容映射;pnpm build 通过 |
| 6.3 | 灵动岛:药丸三态+Dock 括入+拖放入岛 | ✅ | 实施agent(6.3) | 2026-08-14 | 一次 commit(3f8cc46):Island.vue 重写 508 行+IslandDock.vue 490 行;单击 240ms 判定展开(窗口 378↔648 rAF 步进 setSize 280ms)/双击呼出主面板/指针位移>4px 判拖动;岛 Dock=唯一 Dock 载体(真实图标启动预热零延迟/运行绿点 2s 轮询/悬停 ✕ 删/点击启动脉冲+收回+双层防连点/＋提示/溢出前 9+…浮层);拖入 exe/lnk 自动展开添加(去重);onMoved 防抖落盘 island_save_geometry+广播 island-geometry 供主面板跟随;pnpm build 通过;旧 Dock.vue 已删(283d3c9) |
| 6.4 | 主面板:五视图合一+打字即搜+位置跟随 | ✅ | 实施agent(6.4) | 2026-08-14 | 两次 commit(221a738+61b180c 错误台账):MainPanel.vue 576 行(header 二态标题/输入框+5 视图按钮+KeepAlive 缓存 4 视图);视图 id 契约 small-desktop(默认)/search/clipboard/ai/settings;打字即搜(可见字符唤醒,焦点在输入控件时不劫持,IME 组合豁免);Esc 两级(搜索→小桌面→关窗);panel-open-view 与 tauri://show 双时序收敛(pendingView 标记);island-geometry 跟随(水平对齐岛中心,clamp 显示器);onResized 防抖落盘尺寸(位置不落盘);DrawerPanel/ClipboardPanel/AIPanel/SearchBar 内容迁移 views/ 四组件功能零丢失,4 旧组件退役删除;327/327+pnpm build 全绿 |
| 6.5 | 设置页:皮肤区块+emoji 清剿 | ✅ | 实施agent(6.5) | 2026-08-14 | 一次 commit(69206d8):主题区块改皮肤 4 胶囊(深空极光/午夜/拂晓/翠野,theme_mode 随皮肤推导写入 dawn→light 其余→dark)+强调色新 4 色值;删无消费方的搜索框显示方式区块;emoji 清剿(✨→✦/▶→chevron 图标,注释保留);327/327+pnpm build 全绿 |
| 6.6 | 集成收尾 | ✅ | 主对话 | 2026-08-14 | ① 跨窗口皮肤热生效(2609d42):config_save/config_import 成功后全局 emit config-saved,岛窗口监听重载 apply_theme——补上 6.5 发现的"皮肤只对 search 窗口即时生效"缺口;② 全量质量门 cargo test 327/327+pnpm build 全绿;③ release 构建与内存复测见下一步行动 21 条;④ 文档同步(本文件/README/真机验收总清单) |

## 关键决策记录(ADR)

| 日期 | 决策 | 原因 |
|---|---|---|
| 2026-08-11 | 技术栈定案:Tauri2 + Rust + Vue3-TS + Tailwind | 文档规格要求;UI 效果/内存 120MB/多窗口透明置顶三项 Python 方案不达标 |
| 2026-08-11 | Python 全栈方案弃用(评估过 PyQt6/pywinauto 组合) | 灵动岛/Dock 类 Web UI 在 Qt 下开发效率和效果打折;内存目标达不到;热键/剪贴板/壁纸缺现成插件 |
| 2026-08-11 | 仓库建在 D:\github\show,主分支 main | 用户指定目录 |
| 2026-08-11 | 项目名定为 **Aurora(极光)**,bundle 标识符 com.aurora.desktop(Nexus→Atlas→Aurora 两次更名后定稿) | 用户对 Nexus 不满意;Aurora=顶部常驻灵动岛即"极光带",视觉符号与名字互相成就;README 故事线"极光之下,桌面宇宙";开发文档已全局替换 |
| 2026-08-11 | 多 agent 协作:本进度文档为唯一权威进度源 | 多 agent 无共享记忆,须以仓库文档协调 |
| 2026-08-13 | 定位升级:**日常主力工具**,不是玩具/demo | 用户明确要求。后续所有迭代以「主力工具」为准绳,验收维度=开机即用零打扰、高频路径深打磨(搜索/呼出/剪贴板优先于边缘功能)、稳定性(不崩/不丢数据/优雅降级)、可维护(日志可查/配置可迁移/更新可靠)、性能余量持续达标 |

## 下一步行动(Next Actions)

1. ✅ 开发环境已就绪(2026-08-11):Rust 1.97.1 + VS Build Tools 14.44.35207 + Windows SDK,链接链路全链路验证通过
2. ✅ Phase1 手动验收 6/6 通过(2026-08-12):① 热键呼出/隐藏 ✅;② 搜索打开+空态 ✅;③ 托盘隐藏/显示/退出+无残留 ✅;④ 不抢焦点 ✅(代码审核:island focus:false 配置+全仓无 island 焦点调用);⑤ 配置重启生效 ✅(代码审核:18 处变更点均 save 落盘+往返单测);⑥ 内存基线 ✅(release 实测 10/20/30s:WS 44.7/49.1/49.0MB、PRIV 19/20.7/20.7MB,稳定达标);**期间修复 3 缺陷**:灵动岛点击只呼出不隐藏(新增 toggle_search,d22ea5f)、托盘显示全部带出搜索框+漏恢复 dock(02b7a19)、显隐改快照语义(隐藏记可见窗口/显示按快照恢复,ec2859b);README Phase1 已标 ✅
3. ✅ Phase2 手动验收(2026-08-12 收口):Dock(2.1)新形态✅(搜索框底部图标排,验收期持续使用:拖拽添加/悬停删除/点击启动/开关热生效)+抽屉(2.2)✅(见 14 条:分类展开/公共桌面合并/Dock 化/性能)+剪贴板(2.3)✅(用户实测:复制自动记录/搜索过滤/Enter 回贴自动关窗/清空/重启持久化)+壁纸(2.4)✅(c0316ee 预览修复后复测通过,壁纸路径已显示)+系统状态(2.5)✅(用户实测:时间/CPU/内存/网络实时刷新,数值随负载合理)+搜索框可用性✅(2026-08-12:可拖动/可缩放/毛玻璃或不透明可选/几何记忆,见 15 条)
4. ✅ Phase2 集成收尾已完成(2026-08-11):invoke_handler 切真实实现+删 stubs.rs+setup 接线+App.vue 分流+热键/托盘动作+tooltip 订阅;cargo test 77/77+pnpm build 通过;见 2.0 行备注
5. ✅ 远程仓库已推送(2026-08-11,走 127.0.0.1:7897 代理;**注意:当前网络直连不通,须走代理**,与 git-push 记忆相反,以实际网络为准)
6. ✅ Release 构建 + 空闲内存基线复测(2026-08-11):`cargo build --release` 99.2s 成功(exe 16.1MB vs debug 28.3MB);启动后 10s 稳定,10s/20s/30s 三采样点 WS 37.5/37.5/37.6MB、PRIV 15/15/15MB,无持续增长;**达标(<120MB,富余 ~82MB),Phase3 前置解除**;debug 基线超标问题由 release 优化自然解决,Phase3 无需为此专项优化
7. ✅ Phase3 集成收尾已完成(2026-08-11):骨架(ai_panel 窗口+capabilities+依赖+AppConfig AI 字段+密钥脱敏+热键/托盘入口+Settings AI 区块)合入后,3.1~3.3 模块已并行完成并合入 main;集成收尾一次接线:invoke_handler 注册 ai_chat_stream/ai_chat_completion/ai_execute_tool/ai_search_files+commands/mod.rs 声明 ai/file_search+新建 ai/mod.rs(声明 client/tools)+App.vue 挂载 AIPanel;cargo test 142/142(82+16+12+32)+pnpm build 通过;release 构建 + 空闲内存复测:启动后 10s/20s/30s 三采样点 WS 37.5/37.5/37.5MB、PRIV 15.3/15.3/15.3MB,与接线前基线一致,**AI 零常驻资源确认不劣化,达标(<120MB)**;3.1 agent 已连测 Ollama 真实对话链路(流式/非流式/工具分片)
8. ✅ Phase3 手动验收全部通过(2026-08-12 用户实测):① DeepSeek 流式对话 ✅(key 从 ~/.claude/settings.json ANTHROPIC_AUTH_TOKEN 读入本机配置,官方 API 实测 200;key 仅存本机不入库)② Ollama 本地对话 ✅(修复 502 后)③ "打开记事本"工具调用 ✅④ "查找桌面 pdf 发票"文件搜索 ✅⑤ 热键 ctrl+alt+a ✅⑥ 断网/停服务错误条不崩溃 ✅;**502 根因修复(a0abfe2)**:reqwest 默认启用 Windows 系统代理(127.0.0.1:7897)把本机 Ollama 请求转发给代理 → 代理返回 502(实测直连 200/走代理 502);修复=ollama 客户端显式 no_proxy(),DeepSeek 保留系统代理;README 与看板 Phase3 已标 ✅
9. 🔵 Phase5 手动验收清单(待用户,2026-08-12):① 设置→更新:手动检查(无新版本提示),6 小时自动检查待观察;② 部署一个 latest.json+NSIS 到本地静态服务器 → 检查→下载→安装全链路(可先跳过,放后续);③ 设置→壁纸:多屏开关/拼接模式/每屏素材下拉,双屏实机逐屏生效;④ 拔插显示器 → 壁纸自动重建;⑤ AI 面板说"把桌面壁纸换成 xxx" → 动态壁纸工具生效;⑥ 深色主题下 Settings 更新/多屏区块显示正常;验收通过后 README 与看板 Phase5 标 ✅
10. ✅ Phase5 集成收尾已完成(2026-08-12):实现 Task1~9 已全部合入 main(见 Phase5 看板);全量回归 cargo test 223/223 + pnpm build 通过 + 构建 0 警告;release 空闲内存复测 10s/20s/30s 三采样点 WS 38.3/43.7/43.7MB、PRIV 15.9/18.8/18.8MB,**达标(<120MB)**;README 中英双版本与开发进度已同步;docs/代码签名接入.md 已建(签名=可选,仅消 SmartScreen);✅ 已推送 origin main(0d68dc4,直连通)
11. 📋 新需求候选(2026-08-12 用户提出):**搜索框直接搜文件**(输入文件名出结果,非 AI 自然语言;设计文档 §6.3"应用优先,其次文件"的完整搜索目标;Phase1 目前只搜应用,文件搜索在 Phase2 抽屉/Phase3 AI 文件搜索已有部分能力);待用户定优先级后走设计流程
13. ✅ Dock 并入搜索窗口已实现(2026-08-12):**需求起源**:独立悬浮 Dock 与 Windows 任务栏位置重叠、功能重复;用户定调"取消这个dock,直接改为将图标拖拽进去"。**实施(一次 commit)**:删除独立 dock 窗口(tauri.conf.json+lib.rs setup/win_utils LABELS 去 dock);dock.rs 删 dock_get_workarea 命令与 auto-hide 线程(保留数据命令+图标+启动),删对应 2 单测;AppConfig 删 dock_position/dock_auto_hide(#[serde(default)] 兼容旧配置);Dock.vue 重写为 SearchBar 底部内嵌图标排:拖拽 exe/lnk 进 Dock 区添加(onDragDropEvent 官方 API,Tauri 默认拦截 DOM 拖放;drop 位置落在窗口底部 64px 内才算添加,避免误拖)+悬停 ✕ 删除+点击启动/聚焦+绿点运行指示(2s 轮询)+内部拖拽排序保留;空态"拖拽应用到这里";enable_dock 开关每次窗口显示时重读,设置改动下次呼出生效;**质量门: cargo test 221/221 + pnpm build 全绿**;**✅ 2026-08-12 修复拖入 lnk 图标不显示:ExtractIconExW 对 .lnk 返回 0(实测 NoMachine.lnk),改为 lnk 先 resolve_lnk_target 解析目标再提取(曾试 SHGetFileInfoW,并发下偶发 ok=1 hIcon 为空弃用);新增回归单测 extract_icon_from_real_lnk,全量 222/222**;**调试结论(2026-08-12,右键"没反应"双根因)**:① 初始定位压任务栏被遮挡(已修=任务栏矩形方案,实测重叠 0px)② 右键菜单加高窗口失败=capabilities 缺 `core:window:allow-set-size`,菜单 DOM 已渲染但被 64px 窗口裁剪不可见——CDP 实测 contextmenu 事件正常派发(之前"CDP 验证代码正常"是假象:只查了 DOM 没查窗口高度);新形态放搜索窗口内不需加高窗口,天然规避此坑;**Phase2 Dock 旧形态验收取消,按新形态验收(待 GUI)**
14. ✅ 抽屉验收期修复+形态升级(2026-08-12):① **分类点击展开**(66db0b7):点空分类右侧只有标题行像"没展开"→点击分类自动展开该组(防折叠残留)+空分类显示"该分类暂无文件";② **合并公共桌面**(50e5a9f):软件安装的快捷方式(联想浏览器/WPS 等 8 个)放 C:\Users\Public\Desktop,资源管理器把两桌面合并显示而抽屉只扫用户桌面→改为双桌面扫描分组合并、同名用户桌面优先;打开路径校验与 watcher 监听同步覆盖两个目录;③ **Dock 化改造**(908c44b,用户要求"参考 dock 改"):文件区文字行→真实图标网格(复用 dock_get_icon,lnk/exe 提取目标应用图标,其余首字符占位,与 Dock 同款),保留分组/折叠/分类 tab/空态;快捷方式归「程序」类(.lnk 加入 PROGRAM_EXTS,不再堆在"其他");④ **性能优化**(用户反馈"运行太慢"):watcher 防抖 200ms→1s(防重扫风暴)、启动后台预热缓存(首次打开抽屉免同步扫描,秒出数据)、窗口呼出 set_always_on_top 幂等、前端图标模块级缓存(刷新重建免重复 IPC);质量门 230/230 + pnpm build 全绿;已推送 origin main

15. ✅ 搜索框可拖动/缩放/样式切换已实现(2026-08-12,用户要求):① **问题**:搜索框无法移动/缩放(无拖拽区+resizable:false),且只有毛玻璃一种显示方式(低透明度时几乎看不清内容);② **实现**(d24f567):左上角拖动手柄(tauri 原生 drag region,两个视图通用)+右下角自绘缩放手柄(pointerdown 追踪 + rAF 节流 setSize,最小 360×260)+显示方式二选一「毛玻璃/经典不透明」(设置页新增搜索框区块,热生效:config-saved 事件即时刷新,设置页自身面板也跟随);位置与大小经 onMoved/onResized 防抖 600ms 写回配置(search_save_geometry,与 config_save 解耦不触发热重载),setup 启动恢复 + clamp_to_visible 越界保护(显示器拔掉/分辨率变化时回退居中,纯函数 4 单测);search 窗口 resizable 置真 + capabilities 补 allow-set-size;③ 质量门:cargo test 234/234 + pnpm build 全绿;已推送 origin main

18. ✅ 自主迭代第四五轮(2026-08-13,定位升级为主力工具后):① **第四轮**(78813f6/e688601):CSP 由 null 补生产配置(style 内联/asset 协议双写/IPC 通道逐项推演,devCsp 单独放宽 HMR);更新器改分块流式下载+update-progress 进度事件契约;壁纸 data URL 收紧 20MB;UI 一致性收尾(圆角/字号统一、29 按钮补 aria、配置加载失败占位、实测修复 Tailwind var 透明度修饰符整类失效的静默 bug、删死代码);② **第五轮·主力工具化**(23738f9/0ae5d89):崩溃日志(panic hook 滚动日志+事件日志,出问题用户可反馈日志)、开机自启(reg.exe 写 HKCU Run+设置开关+注册表校准)、首次启动引导(自动呼出搜索框+托盘快捷键提示+关闭落盘)、异常审计修复两处(配置/剪贴板损坏先备份 .broken 再回退)、搜索拼音匹配(pinyin crate 全 CJK+多音字,jsq→计算器/chongqing→重庆)+最近使用置顶、呼出延迟评估无阻塞点;③ 质量门 cargo test 318/318+pnpm build 全绿;内存基线复测 WS 50.8/PRIV 21.2 达标

17. ✅ 自主迭代第二三轮(2026-08-13):① **第二轮**(48382eb/c807df5):搜索框直接搜文件(需求候选 item 11 实现:search_files 命令白名单/深度上限/提前退出,前端应用+文件双结果组跨组导航)+H3 AI 工具确认流(open_item 执行前 emit 确认事件,前端确认条,60s 超时拒绝;get_clipboard_history 云端禁用仅本地 Ollama 可用;ai_execute_tool 单轮入口同门防护);② **第三轮**(0c8bf2e/ee4da05):壁纸注入全局互斥锁(修多屏探针与 set 命令并发竞态)、剪贴板落盘 2s 节流+监听失败可重试+单条删除命令、update_feed_url 六域白名单校验(config_save 收紧为 Result)、锁毒化统一;前端剪贴板单条删除(悬停 ✕)、热键录制输入(点击按组合键生成后端格式)、Dock 末尾 + 占位提示、设置页 KeepAlive 保活;③ 质量门 cargo test 288/288(278→288)+pnpm build 全绿;**真机验收总清单已整理**([真机验收总清单.md](./真机验收总清单.md),可自动化项全部单测覆盖,真机项分 A 快速组/B Phase4/C Phase5)

16. ✅ 自主迭代第一轮(2026-08-13):**发布 + 三线审计修复**,用户定调"不参与、多 agent 自主迭代",主对话只管控。① **v0.1.0 首发**:tag+Release 发布(exe 4.6MB/MSI 7MB/latest.json 三资产),latest.json 入库使自动更新默认源开箱可用(bf0ffd7);② **三线侦察**:前端 UX 审计 26 项/Rust 安全审计 15 项/文档一致性审计,修复**主题即时生效接线**(apply_theme 从未被调用的核心缺陷)、语义色令牌化(浅色主题可读)、ToggleSwitch 抽取、SearchBar 六项体验(滚动跟随/真实图标/最近使用/两级 Esc/设置页 Esc/加载错误态)、Dock 轮询随窗口显隐、破坏性操作确认、壁纸缩略图 4 路并发、抽屉图标懒加载;③ **Rust 安全加固**(2d3baf1):html 壁纸 IPC 洞封堵(白名单移除 html,双层堵洞)、SSE 分片 idx 上限 32 防 OOM、符号链接逃逸封堵、图标位图 512 上限、AI base_url 入参移除防 key 外流、文本注入 20000 上限、缩略图 64MP 上限、更新安装 sidecar 哈希复验+cmd 注入面收敛;④ **并发持久化**(cf9ae06):配置读写锁、tmp+rename 原子写、drawer watcher 竞态 compare_exchange、应用索引 60s 增量重扫;⑤ 质量门 cargo test 255/255(237→255)+pnpm build 全绿;README 中英双版补齐(快捷键 Ctrl+Shift+H/模块默认关闭说明/Dock+主题+自动更新卖点/热生效原则);⑥ **遗留待办**:H3 AI 工具确认流(open_item 执行前用户确认)、Authenticode 签名校验(等证书)、搜索框直接搜文件(用户需求候选,优先级待定)、Phase4/5 真机验收项(清单 docs/Phase4-手动验收清单.md,自动化分析结论见 3 线审计报告)

19. ✅ 真机验收反馈第一轮修复(2026-08-13):① **P1 按钮选中态**(用户报「点击没反应/颜色不切换」):CDP 实测 dev 必现、release 不复现,双根因——WallpaperPanel.vue watch immediate 回调引用 const stopSync 命中 TDZ 抛 ReferenceError 中断渲染 flush(dev 打开设置面板时 cfg 已加载必崩);store.load 整对象替换与用户点击竞态,旧快照覆盖新值(loadId 序号只接受最新 + lastSaveId 在 save 发起时标记,本地修改优先)。修复后 dev+release 双环境竞态窗口点击全切换、异常 0;按钮选中态同步升级(蓝底白字+同色系投影+按下缩放,选中/未选中对比拉满)。② **P2 搜索框拖动**:可拖区结构无误(根容器 true/列表与设置内容区 false),根因=可拖区域无视觉提示;已加 header 拖拽把手 ⠿+底部「⠿ 拖动窗口」提示+设置页标题栏把手(悬停提亮),列表/设置内容区保持 false 保护滚动。③ **UI 奢华感升级**:细窄半透明滚动条(全局)、输入光标 accent 色、结果列表选中左侧 accent 竖条+hover 提亮、设置页区块标题/ToggleSwitch label accent 前缀竖条、开关滑块微投影。④ **多窗口拖拽补齐**(625d223):AI 面板与剪贴板此前完全不能拖动(无 drag-region),已加根容器拖拽区+内容区禁拖保护+把手;抽屉补把手视觉;三窗口与搜索框体验一致。⑤ 质量门 cargo test 325/325 + pnpm build + tauri build 全绿;release CDP 回归通过。⑥ 测试期间误改的 config 已恢复用户原值(ollama+light)。**✅ 2026-08-14 用户真机复验三项全通过(按钮选中态/窗口拖动/配置完好),第一轮反馈闭环**

20. ✅ 真机反馈第一轮闭环 + P3/P4 收尾(2026-08-14):用户复验三项全通过;P3 收尾=验收流程统一 release 已落地,首启优化挂 v0.2.0 候选;P4 复评通过(未再报观感问题);下一步=Phase4/5 真机验收清单执行 + v0.2.0 评估
21. ✅ Phase6 UI 重构实施完成(2026-08-14):用户反馈"UI 太基础/体验别扭/搜索不能是主界面"驱动的整体重构;形态=一岛一窗(岛药丸三态括入 Dock+主面板五视图小桌面为主+搜索=打字动作+位置跟随岛+4 皮肤+极光缘签名元素);设计经多轮预览稿迭代用户批准后落地(设计稿 d:\2605\aurora-v02-preview.html);6 任务三波多 agent 实施,全量 327/327+pnpm build 全绿;**待用户真机验收**(清单见真机验收总清单 Phase6 七条);验收观察项:岛展开态实际可视约 6 个瓦片(阈值 9 或需下调);验收通过后打 v0.2.0
22. ✅ 真机反馈两轮修复(2026-08-14,aea86eb/fef3a09):第一轮 7 项(双击面板不在岛下方=open_search 未走定位链路;Dock 只显一个=收起态括入区无隐藏样式被挤成 34px;双击区域限制=click 事件被拖动吞改 pointerdown 计数;整岛拖动=静态元素标 drag-region;透明看不清/质感差=默认不透明实色+恢复玻璃选项;CDP 实测验证面板居中偏差 0.5px);第二轮 4 项(设置打开卡=Settings 常驻挂载预热;药丸外方块=透明窗口裁剪外阴影改内高光+面板深空光晕;主面板加 ✕ 隐藏按钮;AI 代码块硬编码适配皮肤)
23. ✅ **v0.2.0 已发布**(2026-08-14,5f10bfa):全新形态「一岛一窗」;NSIS 4.8MB+MSI 7.3MB 双资产;latest.json 已更新(自动更新源生效);Release 页 https://github.com/sssst1118/aurora-desktop/releases/tag/v0.2.0
24. ✅ **岛 Dock 溢出阈值动态化**(2026-08-14,真机验收观察项):静态 `MAX_VISIBLE=9` 高于实际可视容量(~6 瓦片,378 药丸左段时间/状态占位后剩 ~248-270px),第 7~9 个瓦片被 `.island` overflow:hidden 裁剪且不进「…」浮层**直接丢失**;改为 ResizeObserver 按 mini-dock 实测宽度算可视容量(34px 瓦片+4px gap,预留 ＋ 与「…」按钮位,无溢出/有溢出两档公式),左段内容宽窄/DPI 自适应;**CDP 实测双场景**:注入 10 条 → 前 4 瓦片+「…」+浮层列全 10 条目、5 条 → 全显 6 元素无「…」,两场景 clipped=[] 零裁剪;cargo test 327/327+pnpm build 全绿;测试期间注入的 config 已恢复用户原值
25. ✅ **岛三处真机反馈修复**(2026-08-14,用户报:四角方角+圆角并存/拖动右侧闪烁/双击呼出两次后拖不动):① 四角=backdrop-filter 按矩形采样,圆角外四角仍有模糊输出 → `.island` 加 `clip-path: inset(0 round 999px)` 裁进圆角,**PIL 像素级验证四角 alpha=0 完全透明**;② 拖动闪烁=透明窗口+毛玻璃系统拖动每帧重绘 → 位移 >4px 判定拖动后加 `.dragging` 临时禁 blur 换 `--aurora-panel-solid` 实心面板,pointerup 立即恢复+移动断流 150ms 兜底;③ 双击后拖不动=双击强制展开岛,Dock 区显式禁拖(保图标交互)占展开态约 40% 宽度 → 呼出主面板 450ms 后(面板 pop-in 260ms 完成)岛自动收回,恢复窄态可拖;CDP 实测四项全过(双击展开→450ms 收回/拖动 class 出现与消退/clip-path 生效);cargo test 327/327 无回归;**踩坑见错误记录 2026-08-14 两条**(直编 exe+残留 dev server 假正常、模板 class 绑定漏加)

## 多 agent 协作约定

- 每个任务同一时刻只允许一个 agent 领取:领取时把状态改为 🔵 + 填负责人;
- 任务完成:状态 ✅ + 更新时间 + 一句话完成说明;
- 遇到阻塞:状态 🟥 + 备注写明阻塞原因,不要默默跳过;
- 跨 agent 依赖(如 1.1 是 1.2~1.5 的前置)标注在备注;
- 修改本文件后必须 git commit(中文 commit message),推不推远端视情况;
- 找不到参考实现时,先看 [docs/开发文档.md](./开发文档.md) 第 11 节参考项目清单;
- **犯错必记**:踩了坑(报错/返工/误操作)必须追加到 [docs/错误记录.md](./错误记录.md),铁律,防止其他 agent 重复踩坑。
