# Aurora 错误记录(踩坑台账)

> **目的:犯过的错不重复犯。**
> **铁律一:任何 agent 犯错(报错、误操作、返工)后,必须在此追加一条记录并提交 git。**
> **铁律二:新 agent 开工前必读本文件,连同 [开发进度.md](./开发进度.md)。**

## 记录格式

```
| 日期 | 错误 | 根因 | 教训/防再犯 |
```

## 错误记录

| 日期 | 错误 | 根因 | 教训/防再犯 |
|---|---|---|---|
| 2026-08-12 | AI 面板发消息报"模型服务错误(HTTP 502)无详细信息"(Ollama 模式) | reqwest 0.12 默认启用系统代理(Windows 读注册表 ProxyServer=127.0.0.1:7897),把 http://127.0.0.1:11434 本机请求也转发给代理,代理对 127.0.0.1 上游返回 502(实测:curl 直连 200 / 显式 --proxy 7897 复现 502);curl 测不出因 Git Bash 环境变量代理为空 | 本机地址(127.0.0.1/localhost)的 reqwest 客户端一律显式 .no_proxy()(ClientBuilder);公网 API(DeepSeek)保留系统代理;此类"curl 通但应用不通"先怀疑系统代理 |

| 2026-08-12 | pnpm tauri dev 启动报"全局热键注册失败(HotKey already registered, Ctrl+Shift+Space)" | 后台运行着 release 版实例(刚重启过应用又跑 dev),两个实例抢同一全局热键,后启动的注册失败(仅热键失效,应用本身能跑) | 跑 dev 前先清掉运行中的 aurora-desktop 进程(Stop-Process -Name aurora-desktop -Force);热键注册失败日志不代表应用坏了 |
| 2026-08-12 | 壁纸设置区预览图全部"预览不可见" | tauri.conf.json assetProtocol scope 写了 `$USERPROFILE/Pictures/**`——但 Tauri 的 scope 变量展开只认自带变量集($HOME/$PICTURE/$CONFIG 等,源码 BaseDirectory::from_variable),`$USERPROFILE` 不在其中,被当字面路径,glob 永远匹配不上真实文件 → 所有 asset 请求 403 | scope 改 `$HOME/Pictures/**`($HOME 在 Windows 即用户目录;4d20677)。**教训:tauri 配置/路径里的变量一律用 Tauri 变量集($HOME/$PICTURE/$APPDATA),别用 Windows 环境变量名($USERPROFILE)**;后续(3ffda88→c0316ee):asset scope 无公开运行时扩展 API(asset_protocol_scope 私有、Scopes.asset_protocol pub(crate)),自定义目录(如 C:\ProgramData\Lenovo\Themes)仍 403 → **弃用 asset 预览,改后端 wallpaper_thumbnail 缩略图命令**(image 缩到 480px JPEG base64 data URI,任意目录可读,失败占位);DynamicWallpaper 素材仍走 asset 协议不受影响 |
| 2026-08-12 | 搜索框设置界面拖右下角缩放手柄,窗口直接跳回搜索框(设置被强制关闭) | 面板状态重置绑在 onFocusChanged(focused=true) 上:拖缩放时窗口被反复 setSize,透明置顶窗口激活状态抖动触发 focused 事件 → onShown() 无条件关设置。搜索/剪贴板/AI 三面板同款模式 | 重置/刷新时机改绑 tauri://show 事件(仅 hide→show 触发),与聚焦解耦;三面板一并修正(08a2058) |
| 2026-08-11 | 用户报告启动后两个窗口(island/dock 区)空白、点击无反应、疑似滚动条"滑轮";前端产物/dist/App.vue 分流/config store 全部核查正常;重新 `pnpm tauri dev` 后自愈,内容全部显示 | 未捕获到现场报错(根因未定位);疑 vite 首启未就绪时窗口先弹出、或上次 dev 残留进程 | 遇"空白窗口"先确认 vite 是否就绪/干净重启 dev,再排查代码;已核查项(frontendDist/dist/分流逻辑)勿重复排查 |
| 2026-08-12 | 用户实测:Dock 栏右键点击"都没反应"(上次 08-11 已修过空态 handler + 加高窗口,CDP 注入 contextmenu 也验证菜单能弹出,仍无效) | **双根因**:① dock 初始定位 `height-64` 没算任务栏:窗口 T1024 B1097 与任务栏 T1019 B1067 重叠 43px,任务栏 z 序高于置顶窗口,重叠区鼠标事件全被任务栏吃掉;② 修完遮挡后右键仍无效:capabilities 缺 `core:window:allow-set-size`,openMenu 的 setDockHeight 加高窗口失败 → 菜单 DOM 已渲染但被 64px 小窗口裁剪,视觉"没反应"(CDP 挂监听实测 contextmenu 事件正常派发,链路没断) | ① 窗口定位基准必须与 set_position 同坐标系:SPI_GETWORKAREA 语义随进程 DPI awareness 变化(本机 150% 缩放实测偏 30px),改用 GetWindowRect(Shell_TrayWnd)+GetSystemMetrics 同进程自洽;高度用 outer_size 实测(配置 64 实际 73,含 DWM 扩展)② CDP dispatchEvent 是合成事件,验证弹层时必须同时查窗口尺寸变化,不能只看 DOM 有没有③ 前端用 setSize 等写操作 API 前检查 capabilities 白名单(core:default 不含 set-size) |
| 2026-08-11 | 用户实测:Dock 空态显示"右键添加应用",但右键点击毫无反应,添加不了应用 | ① 空态下空白处无 contextmenu handler:根容器只有 `.prevent`(右键被吞),菜单只在图标条目上绑定,空态无图标 → 什么都不会弹;② 菜单(~200px)/添加面板(~270px)超出 64px 高的 dock 窗口被裁剪,即使弹出也看不见,Phase2 从未 GUI 验收过故首次暴露 | GUI 交互缺陷只能靠手动验收暴露;窗口内 absolute 弹出层尺寸必须 ≤ 窗口,不够就临时加高窗口(setSize+setPosition,关闭恢复);空态/空白处的右键同样要接 handler(openMenu 的 item 可空);连续开关菜单的 setSize 要串行化防乱序 |
| 2026-08-11 | `git push -c http.proxy= ...` 报 usage 帮助 | `-c` 配置项必须放在子命令**之前**:`git -c http.proxy= push ...`,放后面被当作 push 选项 | 写命令前确认参数位置;报 usage 说明参数解析失败 |
| 2026-08-11 | 按旧记忆"绕代理直连"执行 push 超时失败 | 网络状况是变量:这次直连 github.com 不通,走 127.0.0.1:7897 代理才通 | **网络状况以实测为准**;先试直连,不通立刻试代理 7897,不要纠结 |
| 2026-08-11 | 首次 push 被拒(远端有本地没有的提交) | 在 GitHub 网页建仓库时默认勾选了 LICENSE/README,GitHub 自动生成了 initial commit | 新建远程仓库后先 `git fetch` 看远端内容,再 `pull --rebase` 合并;LICENSE 内容相同会被自动跳过 |
| 2026-08-11 | 项目名连续更名两次(nexus→atlas→aurora),文档全局替换返工 | 名字没定稿就动手替换,用户随后改主意 | **命名/方向类决策先跟用户确认到定稿再全局替换**;替换用 sed 全量改是快的,但每次返工都浪费时间 |
| 2026-08-11 | 建库时远端自动生成的文件与本地内容冲突(同上) | 本地先写了 LICENSE,远端也生成了 LICENSE | 建库选择"不要自动添加 README/license/.gitignore",或先建空库再推 |
| 2026-08-11 | 计划里的参考仓库 URL `ZeroLaunch-rs/ZeroLaunch-rs` 克隆 404 | 组织名当成了用户名,实际仓库是 `ghost-him/ZeroLaunch-rs` | 克隆 404 先 WebSearch 核实真实仓库,不要猜;核实结果已写入 docs/参考项目调研.md |
| 2026-08-11 | `pnpm create tauri-app` 交互卡死无输出 | 交互提示在非交互 shell 里等待输入;且 registry 直连超时 | 用 `pnpm dlx create-tauri-app@latest ... --yes` 绕过 create 交互层;网络不稳时先 export http_proxy/https_proxy=127.0.0.1:7897 再装 |
| 2026-08-11 | pnpm install 大量包下载超时(代理下并发下载被打爆) | 并发连接数过高,代理扛不住 | 装依赖加 `--network-concurrency 3`(低并发重试即通) |
| 2026-08-11 | pnpm 11 忽略 esbuild 构建脚本(ERR_PNPM_IGNORED_BUILDS) | pnpm 11 的依赖构建安全机制;package.json 的 `pnpm` 字段**已废弃不读** | 在 `pnpm-workspace.yaml` 写 `onlyBuiltDependencies: [esbuild]`,再 `pnpm approve-builds --all` 或 `pnpm rebuild esbuild`;别往 package.json 里写 pnpm 字段 |
| 2026-08-11 | 计划文档 search 排序单测断言写反(`r[0]=="zoom-camera"`) | 大小写不敏感字典序下 "zoom" < "zoom-camera"(短前缀在前),排序结果与计划断言矛盾,属计划文档笔误 | 计划断言与实现语义冲突时以实现为准,修正断言并记入台账(实现=to_lowercase 字典序) |
| 2026-08-11 | tauri-plugin-global-shortcut 的 `on_shortcut` 返回自身 `Error` 类型,不是 `tauri::Error`,`?` 无法自动转换 | 插件错误枚举是 `tauri_plugin_global_shortcut::Error`(内含 GlobalHotkey/RecvError/Tauri 三个变体) | 返回类型直接写插件 Error;字符串注册 API(2.3.2 版 `on_shortcut<S: TryInto<ShortcutWrapper>>`)可用 `"ctrl+shift+space"` |
| 2026-08-11 | `pnpm tauri dev` 报 `cargo not found`(Git Bash 里 export PATH 也无效) | Git Bash 中 `$USERPROFILE` 展开为 `\Users\gengzhengkun-bjb`(盘符 C: 丢失),拼出的路径无效 | 用 `$HOME`(MSYS 原生 `/c/Users/...`)拼 cargo 路径:`export PATH="$PATH:$HOME/.cargo/bin"`;任务书里"PowerShell 里 cargo 不在 PATH"同样适用 |
| 2026-08-11 | windows-sys 0.59 与旧版 API 差异:GetSystemTimes 在 `Win32::System::Threading`(非 SystemInformation)、FILETIME 在 `Win32::Foundation`、MEMORYSTATUSEX 字段是 `ullTotalPhys/ullAvailPhys`(非 dwTotalPhys) | 计划基于 windows-sys 旧版写成;0.59 模块归位调整 | 按本机 crate 源码修正;写 Windows 调用前先 grep 目标 crate 源码确认符号位置 |
| 2026-08-11 | Phase2 骨架合并三个编译坑:(1) capabilities/default.json 写 `//` 注释,tauri build.rs 报 "failed to parse JSON";(2) capabilities 配 `core:asset:default`,tauri 2.11 报 "Permission core:asset:default not found";(3) `on_shortcut(String)` 报 From<String> 未实现 | (1) capabilities 用严格 JSON 解析器,只有 tauri.conf.json 支持 JSONC;(2) 新版 tauri 的 asset 协议由 config 的 security.assetProtocol scope 静态控制,无需也不存在 capability 权限;(3) on_shortcut 泛型只接受 `&str`/`&Shortcut`,String 无 TryInto | 写 capabilities 前先验证解析器(注释放 description 字段或直接不写);不存在的权限先 grep `~/.cargo/registry/src/*/tauri-*/permissions` 确认;热键字符串传 `&str` 不传 String |
| 2026-08-11 | 2.5 网络采样:设计文档写"加 Win32_NetworkManagement_IpHelper feature 即可用 GetIfTable2",实际编译报错:0.59 里 GetIfTable2/MIB_IF_TABLE2/MIB_IF_ROW2 全部被 `Win32_NetworkManagement_Ndis` feature 门控;且 `windows_sys::core` 不导出 `c_void` | 设计基于旧版 windows-sys 或想当然;0.59 模块门控比文档严格 | 写调用前先 grep 本机 crate 源码确认 cfg 门控与符号位置;Cargo.toml 属集成 agent 不能动 → 在模块文件内手写等价 FFI(布局照抄源码,勿自改);c_void 用 `core::ffi::c_void` |
| 2026-08-11 | 2.5 `pub mod system_sampler;` 写在 src/commands/system.rs 里,文件放在 src/commands/system_sampler.rs,报 E0583 file not found | Rust 模块解析规则:非 mod.rs 的模块文件里的子模块声明只找「同名目录」下的文件,不找兄弟文件 | 子模块文件放 `模块名/子模块名.rs`(如 commands/system/system_sampler.rs),或根级文件用 mod.rs 布局;报 E0583 先按报错提示的候选路径放文件 |
| 2026-08-11 | `pnpm build` 报 ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY,先要重装依赖 | pnpm 的 deps 状态检查发现 node_modules 与 lockfile 不一致,清理需 TTY 确认;非交互 shell 无 TTY | 加 `CI=true` 前缀跑(自动确认清理重装);或 `pnpm config set confirmModulesPurge false` |
| 2026-08-11 | 2.4 与 stubs.rs 同名命令报 E0428(`__cmd__wallpaper_set_static` / `__tauri_command_name_xxx` 宏重复定义) | `#[tauri::command]` 对 pub fn 生成的包装宏带 `#[macro_export]` 导出到 **crate root**,任何两个模块存在同名 pub 命令即冲突(与 stubs 共存期必然发生) | 模块实现函数改后缀名 + `#[tauri::command(rename = "原命令名")]` 保持外部 invoke 名与签名;stubs.rs 删除后可改回原名。2.2/2.3 同样撞此坑,参考同样解法。**变体解法(2.2 剪贴板在用)**:命令函数暂时不写 `pub`(私有 fn 不触发宏导出),集成 agent 切换 invoke_handler 时把 `fn` 改 `pub fn` 即可,函数名可保持与 stubs 完全一致 |
| 2026-08-11 | 2.4 想用 RegGetValueW 读 HKCU\Control Panel\Desktop\WallPaper,编译不可用 | RegGetValueW(advapi32)与 SHRegGetValueW(shlwapi,在 Shell 模块)均被 `Win32_System_Registry` feature 门控,该 feature 未在 Cargo.toml 启用(集成 agent 文件不能动) | 改用 `SPI_GETDESKWALLPAPER`(内部读取同一注册表值,WindowsAndMessaging 已启用);坚持读注册表的话由集成 agent 加 feature |
| 2026-08-11 | 共享 checkout 上其他模块 agent 未提交的半成品文件(drawer/clipboard/system_sampler 等)使整树 cargo test 编译失败 | 5 个模块 agent 共用同一工作区,开发中文件未提交即被全树编译拾取,且各自有未修完的错误 | 验证自己模块用 `git worktree add` 到临时目录隔离编译,完成后删除,不动他人文件;整树编译验收统一留给集成 agent |
| 2026-08-11 | 2.2 classify 单测失败:`.ts` 被分类为"视频"而非"代码" | `.ts` 同时是 TypeScript 源码与 mpeg-ts 传输流扩展名,视频规则表先匹配,桌面场景 TS 源码远常见 | 归类冲突按桌面常见度裁决(ts→代码);规则表冲突先写单测覆盖,扩展名唯一性用单测断言防回归 |
| 2026-08-11 | 2.1 手写 IShellLinkW COM vtable 按"经典多继承 27 槽"布局,运行时段错误;改用 21 槽后 .lnk 创建/解析成功 | 实测 SDK 头文件(shobjidl_core.h)`IShellLinkW : public IUnknown` 是**直接**继承,没有 IPersist/IPersistFile 中间层;IPersistFile 是独立接口须 QueryInterface(IID 0x0000010B)获取,9 槽(Load=槽5/Save=槽6) | 手写 COM vtable 前先 grep SDK 头文件确认继承链与槽位,不要凭记忆猜布局;GetPath=槽3/SetPath=槽20;验证法=对 CoCreateInstance 返回对象调 GetClassID 看是否崩溃 |
| 2026-08-11 | 2.1 .lnk 往返单测断言失败:目标路径 `GENGZH~1`(8.3 短名)vs 解析结果长名 `gengzhengkun-bjb` | `env::temp_dir()` 在本机返回 8.3 短名,外壳保存 .lnk 时规范化为长名;解析结果本身正确(长名恰是 QueryFullProcessImageNameW 的格式) | 涉及 Windows 路径相等的断言用 `std::fs::canonicalize` 两侧归一化后再比;8.3 短名是合法表象,不要改生产代码去适配测试 |
| 2026-08-11 | 集成收尾 TrayIconBuilder 链式 `.id("main")` 报 E0061 参数个数错误 | tauri 2.11.5 中 `TrayIconBuilder::id()` 是**无参 getter**(返回 `&TrayIconId`),id 默认值就是 "main";给 Builder 设 id 是旧版/其他平台 API 记忆 | 写 tauri 调用前先 grep 本机 crate 源码确认签名;不需要自定义 id 时直接不调 `.id()` |
| 2026-08-11 | 集成收尾 `app.tray_by_id` / `app.listen` 报 E0599 方法找不到,且 `Emitter`/`Manager` 导入报 unused | tauri 2 中 `tray_by_id` 是 AppHandle 的**固有方法**(不需要任何 trait),`listen` 在 `Listener` trait(不是 `Emitter`,Emit 系列才在 Emitter);`get_webview_window` 在 `Manager` | 查 tauri 方法所属 trait:固有方法(grep `impl AppHandle`)无需导入;事件订阅用 `tauri::Listener`,发送用 `tauri::Emitter`,窗口/状态管理用 `tauri::Manager` |
| 2026-08-11 | 集成收尾 tray tooltip 订阅闭包报 E0505 cannot move out of `app` because it is borrowed | `app.listen(&self, ...)` 的 `&self` 借用与闭包 `move` 捕获同一 `AppHandle` 冲突 | 监听器模式固定写法:先 `let x = app.clone();` 给闭包用,`listen` 调在原始 app 上 |
| 2026-08-11 | 自写 tray tooltip 单测断言 "CPU 13%" 失败,实际输出 "CPU 12%" | Rust `{:.0}` 格式对 12.5 用**银行家舍入**(round half to even → 12),不是四舍五入 | 写浮点格式化断言前先确认舍入语义;断言写成匹配实际舍入规则的值并注释 |
| 2026-08-11 | 验收冒烟时 PowerShell `Get-Content`/`ConvertFrom-Json` 读 clipboard.json/config.json 中文乱码甚至报 JSON 解析错误 | PowerShell 5.1 默认按系统 ANSI(GBK) 解码 UTF-8 无 BOM 文件,GBK 误读 UTF-8 序列可能产生破坏 JSON 的字节;文件本身完好(app 用 UTF-8 读写) | 读 UTF-8 文件显式用 `[System.IO.File]::ReadAllText(path, UTF8Encoding($false))`;Rust 侧读写均为 UTF-8,乱码只发生在 PowerShell 显示层 |
| 2026-08-11 | 3.2 按设计骨架写 `pub const ALL_TOOLS: &[ToolSpec] = &[ToolSpec{parameters: json!(...)}]`,编译 167 错(E0015/E0493/E0716) | `json!` 宏展开含 Vec/String 运行时分配,**const 上下文不可求值**(设计文档 §2.3 骨架未实编译) | 工具清单类静态数据用 `std::sync::LazyLock`(Rust 1.80+ 标准库,零新依赖),字段类型与消费方式不变;const 上下文只放字面量 |
| 2026-08-11 | rule_match 中文关键词子串重叠:"查找"删"找"残留"查"、"搜索"删"搜"残留"索",单测抓出两处失败 | 单字关键词是组合词子串,逐词 replace 顺序不当 | 关键词表按长度降序排列("搜索"先于"搜"),命中一类后删除该类**全部**命中词再 trim;中文关键词规则先写单测覆盖组合词 |
| 2026-08-11 | 3.3 单测两连挂:(1)深度边界写成 `depth > MAX_DEPTH` 停止,第 4 层目录内容仍被扫(期望 4 命中实际 5);(2) `&hint[1..]` 剥扩展名提示首字符:IMAGE_EXTS/VIDEO_EXTS 存裸扩展名("jpg")而 pdf/word 提示带点(".pdf"),图片/视频过滤永远不命中;(3) JSON 序列化断言漏反斜杠转义 | (1) "递归深度 ≤3"语义易混:根=0 时第 4 层目录 depth=3,必须 `>=` 停止才能保证"第 4 层文件搜不到"(设计 §3.5 手动验收项);(2) 同一函数返回的提示形态不统一(带点/裸扩展名),比较端假设带点导致裸扩展名被砍首字符;(3) JSON 字符串中 `\` 须双写 | 深度/数量类上限先写清"根=0 还是 1、第几层截断"再写判断;扩展名比较统一 `trim_start_matches('.')` 兼容两种形态;JSON 字面量断言注意转义;边界语义先写单测再实现 |
| 2026-08-11 | Git Bash 跑 `cargo` 报 command not found(3.3 worktree 验证首跑踩到) | cargo 不在 Git Bash 默认 PATH;`$USERPROFILE` 拼路径丢盘符(同 2026-08-11 既有记录) | 统一 `export PATH="$PATH:$HOME/.cargo/bin"`(`$HOME` 是 MSYS 原生路径);worktree 首编译用其自带 target,约 1 分钟(不共享主工作区 target,避免与集成 agent 编译撞锁) |
| 2026-08-11 | 3.1 client.chat 返回契约与单测假设不一致,真实 Ollama 工具回复被丢成空串 | 单测用假 client 直接返回完整 message JSON,掩盖了真实实现只回 content 文本;真实工具回复 content 为空串(非 null),extract_completion_text 返回 Some("") → 工具信息丢失,parse_tool_calls 永远 None | 契约类函数必须用真实端点连测一次再收尾;空字符串与 null 一样视为"无文本",回退完整 body 让 parse_tool_calls 可解析(§2.3) |
| 2026-08-11 | 流式 SSE 只收 delta.content,工具调用分片(delta.tool_calls)被丢弃,流式下工具循环永不触发 | OpenAI 流式工具调用经 delta.tool_calls[] 分片传输(按 index,id/name 仅首片,arguments 跨片追加),与 content 是两条独立通道 | 新增 parse_sse_tool_chunk + finish_stream 归并:流结束收集到分片 → 返回构造的 message JSON;纯文本 → 返回 content 全文;工具轮次不发 chunk(§2.3 动作条只走 tool 事件) |
| 2026-08-11 | ToolExecutor `&'a (dyn Fn -> BoxFuture<'a, ...>)` 把 future 生命周期与借用绑死,三处测试报 E0597 "coercion requires 'static" | 闭包实际返回 BoxFuture<'static>(exec_tool_action 持 owned AppHandle),但类型把 future 生命周期写成 'a,迫使借用方升到 'static | 执行器类型里 future 固定 BoxFuture<'static>;借用生命周期单独保留;exec 闭包内 clone AppHandle 保证 'static 可行 |
| 2026-08-11 | client::chat 泛型 on_delta 传 `&mut dyn FnMut` 报 E0277 无大小 + trait 方法 `BoxFuture<'_,...>` 无法同时捕获 &self 与 on_delta | (1) 泛型 F 隐式 Sized;(2) 返回 future 借两个输入引用,elision 只给一个生命周期 | (1) 泛型边界加 `?Sized`;(2) trait 方法显式 `fn chat<'a>(&'a self, ..., on_delta: &'a mut (dyn FnMut + Send)) -> BoxFuture<'a, ...>` 统一生命周期 |
| 2026-08-11 | 3.1 单测 tool_reply 手拼 format 字符串嵌套引号未转义 → 非法 JSON,失败隔离测试假挂(实际 parse_tool_calls None 走"无工具"分支) | `"arguments":"{"path":"C:\x"}"` 内层引号未转义不是合法 JSON,但手拼代码一眼看不出 | 构造 JSON 一律用 `json!` 宏(自动转义),不手拼 format;此坑同时暴露"契约假绿"(见上两行) |
| 2026-08-11 | worktree 首轮验证出现 3 个编译错误波次(Sized/生命周期/HRTB),修复 6 处 | 异步 trait 抽象(Rust 2021 无 async fn in trait)层层加 Send/Sync/'static 约束,错误定位链条长 | 异步 trait 设计先画"future 借谁"图再写签名;`&mut dyn FnMut + Send` 转发要 ?Sized;错误一次看全(cargo test 全量输出),同类错误归因后一起改 |
| 2026-08-12 | 4.1 按设计文档 §1.3 写 WorkerW 注入(EnumWindows 找顶层 SHELLDLL_DefView 取父),实测 Win11 上 DefView 是 Progman 的**子窗口**,EnumWindows 枚举不到;0x052C 后 Progman 出现子 WorkerW | 设计文档 §1.3 基于 Win10 旧机制假设,Win11 桌面窗口结构有差异(DefView 直接挂 Progman 下) | WorkerW 注入以实测为准:主路径 `FindWindowExW(progman, 0, "WorkerW", null)` 找 Progman 子 WorkerW,兜底枚举顶层 WorkerW 含 DefView 子窗口;attach 用 SetWindowPos(HWND_BOTTOM);设计文档 §1.3 与实现已偏差,引用时注意 |
| 2026-08-12 | 4.2 按旧 windows-sys 记忆写 `INPUT { u: MOUSEINPUT }` 编译失败 | 0.59 的 INPUT 是 `INPUT { r#type, Anonymous: INPUT_0 }`,union 成员是 mi/ki/hi(不是旧版 u/mi 命名) | 写 windows-sys 结构体前先 grep 本机源码;INPUT 构造:先 `INPUT { r#type: INPUT_MOUSE, ..zeroed() }` 再写 `input.Anonymous.mi = ...` |
| 2026-08-12 | 4.1 想临时改 lib.rs/commands/mod.rs 验证整树编译,被执行器"共享文件修改拒绝"挡住 | 共享文件所有权由执行器强制,模块 agent 无权限改集成 agent 的文件 | 模块 agent 验证接线:复制整个 src-tauri 到 `.p4check/` 临时副本,在副本内接线编译测试,验证后删除——主工作区共享文件保持原样 |
| 2026-08-12 | 4.1 写 `w!(SOME_CONST)` 编译错 | windows-sys `w!` 宏只接受字符串字面量,不接受常量/变量 | 窗口类名等直接内联 `w!("SHELLDLL_DefView")` |
| 2026-08-12 | 4.1 用 PowerShell 记忆的 `-m @'...'@` 写多行 commit message,Git Bash 把它当字面量首行输出裸 `@` | Git Bash 是 POSIX shell,PowerShell here-string 语法不适用 | Git Bash 多行 commit 用 POSIX heredoc 或 `-m "标题" -m "正文"` 两个参数 |
| 2026-08-12 | 4.2 模块未接线时 automation 全部函数报 dead_code 警告 | 命令未注册进 invoke_handler 前模块无引用(与 Phase3 各模块同型) | 模块合入前的 dead_code 警告是预期,**不要加 #[allow(dead_code)]**,集成 agent 注册后自然消除 |
| 2026-08-12 | 4.3 UiaFind/UiaNavigate 的 ppRequestedData 在 Win11 返回 UIAutomationCore 内部格式(cDims=2/cbElements=24 的周期记录),不是可消费的 HUIANODE;直传 UiaGetPropertyValue/UiaGetUpdatedCache 全部 0xc0000005 崩溃 | UiaFind/UiaNavigate 的句柄式数据通道在本机实测不可消费(UIAutomationCore 内部引用),与文档假设不符 | 遍历改走 Win32 窗口树(EnumWindows/GetWindow 遍历顶层与子窗口)+ UiaNodeFromHandle 建节点 + UiaGetPropertyValue 读属性,链路实测完全可用;UiaNodeFromHandle 节点由 UIAutomationCore 托管不调 UiaNodeRelease(泄漏风险留手动验收,收口在 node_from_hwnd 一处) | 
| 2026-08-12 | 4.3 UiaFind 调用返回 E_INVALIDARG / 访问违规 | `UiaCacheRequest.pViewCondition` 传 NULL / `cache` 传 NULL 均非法 | UiaCacheRequest 的 pViewCondition 必须给真实条件对象(TreeScope_Descendants),cache 必须非 NULL;Uia* API 参数契约与 Win32 风格不同,按文档逐参核对 |
| 2026-08-12 | 4.3 用 PowerShell `Get-Content -Raw \| Set-Content -utf8` 处理中文 UTF-8 源文件,双重编码乱码且字节有损(GBK 孤立字节丢失) | PowerShell 5.1 默认 ANSI(GBK)读写,管道处理 UTF-8 文件时编码转换丢失 | 任何源码/文档编辑一律用专用工具(Read/Edit/Write),不用 shell 管道改文件;误伤后从会话 transcript 完整重放恢复 |
| 2026-08-12 | 4.3 把命令层放进了 `automation/uia_cmd.rs`(纯函数目录),lib.rs 按 `commands::automation::uia_*` 注册编译失败 12 处 | 架构约定:命令层在 `commands/` 目录(4.2 的 commands/automation.rs 即命令层),automation/ 目录只放纯函数封装;4.3 agent 未遵循既有布局 | 合入时 `git mv` 到 commands/ + commands/mod.rs 声明 + lib.rs 改路径;模块 agent 任务书应写明命令层位置;合入前先核对路径假设,别照搬模块内部结构 |
| 2026-08-12 | 集成 agent 后台任务跑 cargo 报 `cargo: command not found` | 后台任务 shell 环境未加载 ~/.cargo/bin(PATH 里没有) | 跑 cargo 前显式 `export PATH="$PATH:$HOME/.cargo/bin"`(Git Bash 用 $HOME 拼路径,别用 $USERPROFILE) |
| 2026-08-12 | 4.5 MSI 构建 WiX 下载失败:直连 os error 10053 连接被中止;手动下载解压到 `%LOCALAPPDATA%\tauri\WixTools`(candle/light 齐全)后构建**仍**触发重新下载 | github release 直连不稳;tauri-bundler 的 verify 未认可手动放置的缓存(校验逻辑与解压目录不完全匹配),且其下载器(ureq)不读系统代理设置 | 构建前 export `HTTPS_PROXY=http://127.0.0.1:7897` `HTTP_PROXY=http://127.0.0.1:7897`(bundler 下载器读 env 代理,实测即通:validating hash→extracting WIX→candle→light 全链路);curl 手动下载用 `-x http://127.0.0.1:7897`;大文件给足 --max-time |
| 2026-08-12 | release exe 手动启动界面空白(灵动岛无内容),dev 模式正常 | `target/release/aurora-desktop.exe` 用 `cargo build --release` 直接构建,**未启用 tauri custom-protocol feature**,运行时仍加载 devUrl(http://localhost:1420),vite dev server 不在即全部窗口白屏;MSI/NSIS 安装包走 `pnpm tauri build` 带 custom-protocol 正常;此前 release 只测内存基线没人看过 exe 界面,坑从未暴露 | **release exe 必须用 `pnpm tauri build`(CLI)构建**(`--no-bundle` 只出 exe 亦可),禁止拿 `cargo build --release` 产物当运行/验收对象;验证窗口加载源:`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS="--remote-debugging-port=9222"` 启动后 curl /json/list,URL=`http://tauri.localhost/` 为内嵌正常,`http://localhost:1420/` 为 devUrl 残留即产物错误 |
| 2026-08-12 | 删掉 App.vue 分流链第一个 `v-if="label === 'dock'"` 后,`label` 变量报 TS6133 从未被读取,第一个 `v-else-if` 分支整体失效 | 第一个分支是孤立的 `v-else-if`(它要求前面必须有配对的 `v-if`),Vue 编译器将其忽略,模板里 label 不再被引用 | v-if/v-else-if 链删掉首项时,把下一项从 `v-else-if` 改成 `v-if`;vue-tsc 的"变量未使用"报错往往是指向模板条件链失效的线索 |
| 2026-08-12 | 形态定调后 Dock 组件重写,残留引用自查:config.ts 的 dock_position/dock_auto_hide、App.vue 的 dock 分支 | 前端删字段/组件后,TypeScript 接口与模板分支不同步会连锁报错 | 删字段后 grep 全 src 残留(接口/模板/注释),一处清干净再跑质量门;废弃字段名保留在注释里说明"已废弃+日期"便于后人溯源 |
| 2026-08-12 | 拖入 .lnk 应用图标不显示(占位首字母):ExtractIconExW 对快捷方式返回 0 | ExtractIconExW 只认 exe/ico,对 .lnk 直接调用失败(实测公共桌面 NoMachine.lnk 返回 0);目标 exe 单独提取全部成功 | .lnk 先 resolve_lnk_target 解析目标再提取(行为=资源管理器显示目标图标);新增回归单测 extract_icon_from_real_lnk |
| 2026-08-12 | 改 SHGetFileInfoW 后全量测试轮番挂(三个图标测试轮流 FAILED),单跑全过 | SHGetFileInfoW(SHGFI_ICON) 在并发调用下偶发 ok=1 但 hIcon 为空(系统图标缓存竞态);且 SHGFI_ICON 隐式依赖 COM 初始化(未初始化时 exe 也拿不到句柄,CoInitializeEx(STA) 后恢复) | 提取入口别用 SHGetFileInfoW,用 ExtractIconExW+目标解析组合(稳定);凡"并发下偶发挂、单跑全过"的测试,先在并发现场抓探针定位(ok/hicon/COM hr 全打印),再决定保留/回退 |
| 2026-08-12 | 加 windows-sys feature 后 cargo 不重编依赖,仍报 unresolved import | windows-sys 0.59 的 SHGetFileInfoW 门控是 `all(Win32_Storage_FileSystem, Win32_UI_WindowsAndMessaging)`,不是 Win32_UI_Shell_Common(Common 只管 SHFILEINFOW/SHGFI_FLAGS 类型) | 加 windows-sys API 前先 grep 本机源码看函数的确切 cfg 门控,别按模块名猜 feature;unresolved import 排查用 `cargo tree -e features` 核对实际启用的 feature |
| 2026-08-12 | 新启动应用无绿点(NoMachine):运行窗口已枚举到但 dock 不亮 | NoMachine 客户端实际运行进程是 `nxplayer.bin`(启动器 nxplayer.exe 拉起后改名),running_windows 的 exe 路径与 lnk 解析目标 `nxplayer.exe` 扩展名不同,path_matches 严格字符串相等永不匹配;同理 dock_launch 的聚焦判断也失效,点击会重复拉起启动器 | 运行匹配加 stem 回退,但**仅限扩展名不同时**(.bin vs .exe):扩展名相同的不同目录文件仍是不同应用,不许回退;路径/扩展名相等的“进程名”假设全不可靠,以实际枚举为准 |
| 2026-08-12 | Dock 点击启动慢(约 2.7s 才有反应) | 链路两处重:resolve_lnk_target COM 解析每次点击都做(实测 1.54s,含首次 COM 初始化);opener::open(lnk) 走 Shell 语义(实测 1.29s)vs 直开目标 exe 仅 16.8ms | lnk 目标解析结果加 OnceLock+HashMap 缓存(条目不变结果稳定,首次 1.5s 一次性,之后瞬时);启动直接 opener::open(解析出的目标 exe)而非 lnk——代价是丢 lnk 参数/工作目录,无参数应用场景可接受,解析失败回退原 lnk 保底 |
| 2026-08-12 | 首次打开搜索栏图标加载几秒(实测冷启动 8 个 lnk 提取 1.94s,首图标独占 1.85s) | 图标提取 extract_icon_pixels 独立调 resolve_lnk_target,**不走 dock.rs 的 LNK_TARGET_CACHE 共享缓存**——每个 lnk 重复付 COM 初始化(首次 1.85s)+完整解析;且成本发生在"打开搜索栏那一刻" | 所有 lnk 解析统一走 item_target_path 共享缓存(启动/提取/匹配同一份);把 1.85s 的一次性成本挪到应用启动时后台预热——实测预热后 8 图标 10ms,190 倍;预热失败静默,打开时现场提取兜底 |
| 2026-08-12 | 预热代码加了仍慢:图标提取成本仍在"打开搜索栏之后" | **两个错**:① App.vue 不是 search 窗口的根组件(main.ts 分流 label==='search' → SearchBar),App.vue 的 onMounted 对 search 窗口从不执行——预热挂错了壳;② enableDock 初始 false,Dock 组件 `v-if` 懒挂载,loadDockFlag 只在 onShown(呼出)时读开关 → Dock 挂载+图标提取发生在用户点击之后 | 改前先确认窗口→根组件的映射(main.ts 分流表),预热/早加载逻辑放对壳;开关初始值别懒读,setup 即读让组件尽早挂载,"呼出时重读"语义用 onShown 保留即可 |
| 2026-08-13 | Tailwind 3.4 下 `bg-[var(--aurora-accent)]/20` 等「var 任意值 + 透明度修饰」**静默不生成任何 CSS**(项目已有此类用法全部失效) | Tailwind 3.4 无法对 `var()` 任意值做 alpha 合成(该语法需 Tailwind 4) | 透明底语义色用 scoped style + `color-mix(in srgb, var(--token) N%, transparent)` 实现;新增令牌统一给纯 hex 值,color-mix 对 hex 完全兼容 |
| 2026-08-13 | GitHub API 创建 release 报 422 `tag_name is not a valid tag` | `target_commitish` 指定的 commit 尚未 push 到远端,API 无法引用不存在的 tag 目标 | 发布顺序:先 git push main/tag,再 API 创建 release;发布脚本别放 Temp 目录(会被清),放工作目录并跑完即删 |
| 2026-08-13 | github.com 主站 git push 首推被 RST(`schannel: server closed abruptly`),误判为被封锁去折腾代理 | 网络波动:github.com 直连时断时续(RST 概率性),api.github.com/uploads.github.com 稳定直连 | push 失败置空代理多试几次(间隔 3s),绝大多数第二三次即通;不要为此开代理;GitHub API 发布/上传资产可放心直连 |
| 2026-08-13 | P1 按钮选中态:用户验收(dev 模式)报「点击没反应/颜色不切换」,但 config.json 已落盘;CDP 实测 dev 必现、release 不复现 | **双根因**:① WallpaperPanel.vue onMounted 里 `const stopSync = watch(..., {immediate:true})`,immediate 回调在 watch 返回前同步执行,回调体内引用 `stopSync()` 命中 TDZ 抛 ReferenceError,中断该次渲染 flush(dev 下打开设置面板时 cfg 已加载、回调走到底必崩;release 下首次引导时面板早开、cfg 未就绪回调 early-return 不崩——精确解释「只在 dev 验收复现」);② 设置面板激活时 onActivated 发起 config_load(整对象替换),用户紧接着点击,晚到的旧快照整体覆盖新值 | ① watch immediate 回调要自停时,stop 句柄用 `let stop = null; stop = watch(...)`,回调内 `stop?.()` 可选调用,严禁直接引用 const 句柄(所有窗口都可能有此模式,全局排查);② store.load 整对象替换必须防过期:loadId 单调序号(只接受最新 load)+ lastSaveId(save **发起时**即标记,本地修改优先于任何更早发起的 load 快照;若等 save 成功才标记,IPC 在飞期间旧 load 返回仍会冲掉新值);saveSafe 失败回滚走 load(force) 无条件覆盖 |
| 2026-08-13 | 用 `cargo build --release` 直编的 exe 启动后 6 个 WebView2 全白(CDP 显示 url=localhost:1420 且 SYN_SENT 连不上 dev server) | 直接 cargo build 不设置 TAURI_ENV_* 环境变量,tauri-build 按 dev 模式处理加载 devUrl,前端资源不嵌入(直编 exe 16.1MB vs tauri build 4.6MB) | **凡是要真跑/CDP 实测的构建一律 `pnpm tauri build --no-bundle`**(它先跑 beforeBuildCommand 前端构建+嵌入资源);cargo build --release 只配做编译验证 |
| 2026-08-13 | CDP 实测「打开设置面板立即点按钮」偶发按钮无反应,误判为产品 bug | 首次打开面板组件冷挂载 >10ms,探针脚本在按钮 DOM 尚未渲染时 `document.querySelector(...)?.click()` 静默 no-op(物理鼠标点不到未渲染的按钮,真实用户不存在此场景) | CDP 模拟点击后必须校验点击真的发生(如点击后立即读 store 值/查 class 变化),或等待按钮存在后再点;`?.click()` 的静默失败会把测试脚本问题伪装成产品 bug |
| 2026-08-13 | CDP 截图写 WSL UNC 路径(\wsl.localhost)后用 Read 读图全部 [Unsupported Image](PNG/JPEG/缩到 480px 均失败) | 环境变化:WSL 9p 共享经 UNC 读图片当前不可用(旧记忆「WSL 直写可读」已失效),Windows 盘又受加密策略限制 | 视觉验证优先 DOM/computed style 数据(颜色值、类名、布局属性均可用 CDP 查全);需要出图给用户时把截图嵌成 Artifact 页面交给用户看,别把时间耗在自读图上 |

| 2026-08-14 | cargo build --release 直编 exe + **残留 dev server 同时在场**时,CDP 实测页面正常(误以为直编 exe 可用) | 直编 exe 不嵌资源按 devUrl 加载;若 1420 有残留 vite 在服务 dist,页面恰好正常,掩盖了坑;vite 消失后页面变 chrome-error 错误页,测试全部作废 | 真跑/CDP 一律 pnpm tauri build --no-bundle;启动前 netstat 查 1420/9222 占用,页面 URL 必须是 tauri.localhost(devUrl=localhost:1420 即不对) |
| 2026-08-14 | 改了 ref/样式/逻辑却不生效(dragging class 在 CDP 实测始终缺失) | Vue 模板 :class 绑定漏加新 ref——编译产物里 class 对象只有旧键(改 class 绑定后 grep 产物确认:me(["island",{expanded, dragover}])) | 模板绑定的新状态必须 grep dist 产物确认出现在 class 绑定里(压缩后函数名会变,但 class 字符串不变);改模板后别只靠 pnpm build 成功就放心 |
| 2026-08-14 | Task 4 新建嵌套 views/ 目录后相对 import 两轮报 TS2307:第一轮 composables/stores 少一级(写成 ../../ 而非 ../../../),第二轮修正时又把 icons/FileDrawer 改过头(它们与 views 同为 components 下二级,正确是 ../../) | 目录层级没画图核对:views 在 src/components/core/ 下,components 内的兄弟目录(icons/FileDrawer)是两级,src 顶层目录(composables/stores)是三级;第一轮报错只盯着报错行改,没有把同类错误一次看全 | 新目录文件写相对路径前先画目录树定层级;TS2307 类报错先收集全部(同型错误批量一次修完),修完立即重跑 build,别一轮改一个 |
| 2026-08-14 | F2 编译三连错:① lib.rs setup 闭包里 `logical_monitors(app)` 报 E0308(函数要 `&AppHandle`,实参是 `&mut App`);② system_sampler.rs `AtomicU64` 未导入(E0433,`AtomicBool` 已导入也不顶用);③ fallback_position 的 `h` 参数未用(warning) | ① tauri 2 的 setup 签名是 `|app: &mut App|`,App 与 AppHandle 是两种类型,不会自动转——需要 AppHandle 时用 setup 开头 `let handle = app.handle().clone()`;② `std::sync::atomic` 每个原子类型是独立导入条目;③ 新函数参数没想清楚就用 | setup 闭包里取 AppHandle 一律走开头 clone 的 handle,不传 app;新增原子类型/其他 std 类型先确认 import;新函数签名未用参数用 `_h` 命名并注释语义(与 clamp_to_visible 保持签名对称) |
| 2026-08-14 | KeepAlive `:include="[组件对象数组]"` 报 vue-tsc TS2322:MatchPattern 只接受 string/RegExp/字符串数组 | Vue 运行时支持 include 传组件对象,但类型定义(MatchPattern)不收组件对象,数组元素类型推到 string | KeepAlive include 用**组件 name 字符串数组**,并给每个被缓存组件显式 `defineOptions({ name })`(script setup 下 name 靠文件名推断,显式写死最稳) |
| 2026-08-14 | cargo test 跑 `commands::dock::tests::running_windows_smoke` 失败:「exe 路径不应重复: C:\Windows\explorer.exe」(336/337,与前端改动无关) | 测试断言设计缺陷:running_windows() 枚举每窗口一行、不去重,同 exe 多窗口(桌面 shell + 用户开着的资源管理器文件夹窗口都是 explorer.exe)是合法常态,「exe 不重复」断言在此时必挂;属环境敏感失败,非产品回归(现场核实:`Get-Process explorer` 确有带标题的文件夹窗口) | 冒烟测试判重键应是 hwnd(同窗口枚举两次才是真 bug),同 exe 多窗口是正常现象;断言「唯一性」前先确认判重键为业务合法唯一键;cargo test 环境敏感失败先查现场(进程/窗口枚举)再判定回归,勿动他人文件 |
| 2026-08-18 | v0.2.4 CDP 验证 5 个假失败,全部是脚本自身问题,产品代码零改动(T1/T2 核心修复其实全对):① T3b「面板没关」假象——panelVisible 探针查 .main-panel-root 存在性,窗口 hide 后 DOM 仍在恒 true;document.hidden/visibilityState 在 WebView2 里也不随窗口隐藏更新(实测恒 visible);② T3a「岛没收起」——单击=展开/收起 toggle,双击呼出后面板 450ms 自动收回岛已收起,再「单击收起」把岛展开;③ T5 录制按钮选择器 null——录制入口是 readonly **input**(aria-label=抽屉热键)不是 button;④ T4 Space 断言方向反——修复语义=按钮聚焦 Space 应**激活按钮**(视图切换),断言「视图不变」期待的是 bug 行为;⑤ head-input 出现用户真实输入的拼音(测试时用户正在电脑上打字,aurora 抢焦点吃进输入) | ① 窗口隐藏探针唯一可靠=Tauri IPC `plugin:window\|is_visible`(win.hide 后返回 false),DOM 存在性/visibilityState 均不可用;② 测 Esc 三级前显式「展开→收起」收敛岛状态;③ 热键录制入口是 input 不是 button(先读代码再写选择器);④ 断言前先读修复语义,别把 bug 行为当期望值;⑤ 测试前清空输入框值+blur 焦点,提示用户测试期间勿打字 |
| 2026-08-18 | B4 安装链路验证:NSIS **安装版** exe 带 `--remote-debugging-port=9222` 命令行参数启动,程序正常但 9222 无任何监听(裸 release exe 同参可通) | WebView2 对命令行参数的透传依赖 wry 解析,安装版(bundle 资源 patch 后)与裸 exe 行为不一致;WebView2 官方标准透传通道是环境变量 WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS,实测必通 | 已安装版本跑 CDP:`$env:WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS="--remote-debugging-port=9222"; Start-Process <安装路径>\aurora-desktop.exe`;冒烟脚本=tools/cdp/cdp-install-smoke.mjs(12 断言:DOM/URL=tauri.localhost/is_visible/config_load/呼出/切视图/Esc/零污染) |

## 已知环境坑(避免重踩)

| 坑 | 说明 |
|---|---|
| git 代理 | 全局代理 127.0.0.1:7897 时开时关。**每次先试直连,不通走代理**,两条都试再报网络问题 |
| Windows 盘加密策略 | Claude 直接读 Windows 盘(C:/D:)上的截图/图片会无法解码。需要看图时,请用户重新截一张发来;自己渲染的产物写到 WSL 原生盘再读回;**WSL UNC 读图也已失效(2026-08-14 实测)——确需"看"渲染结果时,改用 WSL python3 + PIL 做像素级分析(四角 alpha 是否透明/指定坐标颜色),比纯 DOM 数据更接近视觉** |
| PowerShell 管道喂 WSL | 管道会加 CRLF 导致 WSL 命令整行报错;驱动 WSL 用 heredoc(LF 干净) |
| git 提交规范 | 中文 commit message;上传 GitHub 的内容不留 AI 痕迹(无 Co-Authored-By 等署名) |

## 协作纪律(多 agent 通用)

- 状态变了(环境、配置、文件)立即更新 [开发进度.md](./开发进度.md) 并提交;
- 犯了上面没有的新错 → 按格式追加本文件;
- 引用本项目之外的经验时,以本机实测为准,不要照搬记忆。
| 2026-08-14 | CDP 验证面板跟随多次 FAIL(面板不动),误判为产品 bug | 测试方法连环坑:① node -e 命令行里 `` `${TAU}` `` 模板字符串被 bash 双引号展开吞掉(SyntaxError: Unexpected token '.',表达式静默失败);② CDP 注入 `plugin:event|listen` 缺 `target:{kind:'Any'}` + handler 必须经 `transformCallback` 转换(直接传函数报 missing required key);③ `plugin:window|set_position` 程序性移动被 tao 抑制,**不触发 tauri://move**,岛端根本不 emit,测消费端只能手动 emit island-geometry;④ hide→show IPC 不触发 tauri://show(query 残留清不掉,需真实呼出或 Esc 一级清);⑤ 双击测试前若刚有动画残留,animating 门控使双击判定失效 | ① CDP 脚本里的 invoke 表达式写完整字面量,或避免在 bash 双引号内用 ${} 拼接;② 注入监听先验证(注册成功+收到事件)再依赖其断言;③ 测「跟随」测消费端:手动 emit 事件→断言窗口位移;④ 清 query 用真实用户路径(Esc 或呼出);⑤ 交互测试前先状态收敛(单击+等动画结束) |
| 2026-08-14 | 波次3B:在 ai.rs 测试区用 Edit 插入新单测,old_string 跨两行把下一个测试的首行(fake_client 定义)也吞进替换范围,该测试当场损坏 | Edit 的 old_string 锚点贪多:借「下一测试的首行」做定位边界,new_string 里却漏写回去 | 插入新测试只锚定「上一测试结尾的唯一断言行」,old_string 越短越安全;改完对 touched 区域立即 Read 核对;本波靠 cargo test 全绿兜底发现并当场恢复 |
| 2026-08-14 | screenshot-preview.ps1 四连坑:① 找不到 Edge——`"$env:ProgramFiles(x86)\..."` 里 `(x86)` 被 PS 当作变量名一部分(求值成空),x86 路径永远匹配不上;② 反引号续行偶发失效——PS 5.1 按 CRLF 解析,仓库 LF 文件的行尾反引号续行在解析期静默断链,`--virtual-time-budget` 被当 decrement 报 ParserError;③ `$ErrorActionPreference="Stop"` 下 native 命令(msedge)写任意一行 stderr(Edge 的 QQBrowser 导入器/task_manager 无害 ERROR)即终止脚本,`2>$null`/`2>file` 都拦不住;④ 不加 `--user-data-dir` 时 Edge headless 复用用户已开实例,`--screenshot` 静默不产出文件(exit 0 且无报错) | ① PS 环境变量名拼接 x86 目录必须 `${env:ProgramFiles(x86)}` 大括号形式;② .ps1 入库保持 CRLF(本仓库 autocrlf checkout 自动转),代码内干脆不用反引号续行,改参数数组 + splatting(`& $browser @args`);③ 不用全局 Stop,关键路径用 Test-Path 显式检查 + `exit 1`;④ 无头截图/渲染一律 `--user-data-dir=<临时目录>` 隔离实例 |
| 2026-08-14 | CDP 连上 release exe 却看到页面 URL 全是 http://localhost:1420(错误页 title "localhost"),误判"残留 dev 实例"反复杀进程 | **真根因:直接 `cargo build --release` 不经 tauri CLI,tauri-build 缺环境变量时把 devUrl 嵌入产物**,exe 里 grep 得到 "localhost:1420";1420 无服务则 WebView 加载失败显示错误页;父进程链查询证明页面确实属于 release exe(排查中用 PowerShell 查 Win32_Process 父链排除"残留实例"假设) | **release 产物一律 `pnpm tauri build --no-bundle`**(tauri CLI 设置正确嵌入环境变量);验证嵌入正确性可 `grep -c "localhost:1420" exe`(=0 才对);排查"谁在跑"用进程父链(PowerShell Get-CimInstance Win32_Process 沿 ParentProcessId 上溯),不要凭端口猜 |
| 2026-08-18 | v0.2.5 构建连环两错(均为 PowerShell 5.1 编码坑):① `Set-Content -Encoding utf8` 写入带 **BOM**,tauri.conf.json 解析报 "expected value at line 1 column 1"(Tauri/serde_json 不接受 BOM);② 修复 BOM 后仍报 "control character at line 73",根因=**`Get-Content -Raw` 默认按 ANSI(GBK)读 UTF-8 文件**,中文被 GBK 误读再按 UTF-8 写回,双重转码成乱码(U+E000 私有区字符,git diff 只看字节不辨语义,中文行"看似没变"实际已坏,node JSON.parse 定位 line 72 才发现);已提交 bcb6bed 入库的损坏文件靠 git checkout 干净提交(README 等 fa36fa7 / 版本文件 d13e1da)恢复 + Python 重改版本号才救回 | PowerShell 5.1 读文件默认 ANSI、写 utf8 带 BOM,双坑叠加 | **改含中文的文件一律不用 PowerShell Get/Set-Content**:用 Claude Write/Edit 工具、bash sed、或 Python(io.open encoding='utf-8')。若必须用 PS:读 `Get-Content -Encoding UTF8`、写 `[System.IO.File]::WriteAllText($p,$c,(New-Object System.Text.UTF8Encoding $false))`。改完含中文文件后验证:`python -c` 检查 U+E000~U+F8FF 字符数 + node JSON.parse(JSON 文件) |
