# Phase5 实现计划(发布链路 + 多屏壁纸 + AI 工具)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 Phase5 三块:NSIS 安装包+自动更新+签名预留、多屏壁纸(拼接/独立)、AI 动态壁纸工具;并清理全树历史 dead_code 警告。

**Architecture:** 全部遵循项目既有架构铁律——`commands/` 目录放 `#[tauri::command]` 命令层,实现层放纯函数封装;AppConfig 增量一律 `#[serde(default)]` 逐字段回退;windows-sys 符号先 grep 本机 crate 源码;命令层与实现层分离,单测打纯函数层。5.1 自研 updater 模块(不引 tauri-plugin-updater,证书未采购);5.2 复用 4.1 的 WorkerW 注入(每屏窗口 SetParent 到同一 Progman 子 WorkerW,SetWindowPos 定位到各屏虚拟桌面坐标);5.3 扩展 ALL_TOOLS 白名单与 rule_match。

**Tech Stack:** Tauri 2.11 / Rust 1.80+ / windows-sys 0.59 / reqwest 0.12 / sha2 0.10(新增)/ Vue3-TS / Tailwind v3

## Global Constraints

- 所有 commit 用中文,标题 ≤50 字,正文写"为什么改";不留 AI 痕迹(无 Co-Authored-By)
- 推送先试直连,失败用 `git -c http.proxy= -c https.proxy= push origin main`
- cargo 不在 PATH:先 `export PATH="$PATH:$HOME/.cargo/bin"`
- MSI/NSIS 构建需 `export HTTPS_PROXY=http://127.0.0.1:7897`(WiX/NSIS 工具下载)
- AppConfig 新字段必须 `#[serde(default)]` + Default 值 + 老配置回退单测
- 每任务完成:该任务范围内 `cargo test` 全绿 + `pnpm build` 通过(涉前端时)
- 共享文件(lib.rs/commands/mod.rs/tauri.conf.json/Cargo.toml/App.vue)归属集成 agent,任务按其分配改
- 部署/验证多屏需真机双显示器;单机环境该任务标注"待真机验收",不得虚报通过
- 变更文档:每任务合入后同步 docs/开发进度.md 看板并 commit

---

### Task 1: AppConfig 增量字段(5.1/5.2 前置)

**Files:**
- Modify: `src-tauri/src/commands/config.rs`(AppConfig 结构体 + Default + 测试区)

**Interfaces:**
- Consumes: 现有 `AppConfig` 结构(带 `#[serde(default)]`)
- Produces: 新字段 `update_enabled: bool`、`update_feed_url: String`、`wallpaper_multi_monitor: bool`、`wallpaper_span_mode: bool`(后续 Task 直接读写)

- [ ] **Step 1: 写失败测试**

在 `config.rs` 测试区(文件末尾 `phase4_fields_roundtrip` 测试之后)追加:

```rust
    // ---- Phase5 字段(设计文档 §5):老配置缺 Phase5 字段逐字段回退默认 ----

    #[test]
    fn missing_phase5_fields_fall_back_per_field() {
        let p = tmp_cfg("p5partial");
        std::fs::write(&p, r#"{"hotkey_search":"ctrl+alt+z"}"#).unwrap();
        let cfg = load_from(&p);
        // 5.1 更新:默认开 + 默认 GitHub 源
        assert!(cfg.update_enabled);
        assert_eq!(cfg.update_feed_url, "https://raw.githubusercontent.com/sssst1118/aurora-desktop/main/latest.json");
        // 5.2 多屏:默认关(= 现状只铺主屏),拼接模式默认开
        assert!(!cfg.wallpaper_multi_monitor);
        assert!(cfg.wallpaper_span_mode);
        let _ = std::fs::remove_file(&p);
    }

    #[test]
    fn phase5_fields_roundtrip() {
        let p = tmp_cfg("p5roundtrip");
        let cfg = AppConfig {
            update_enabled: false,
            update_feed_url: "https://example.com/aurora/latest.json".to_string(),
            wallpaper_multi_monitor: true,
            wallpaper_span_mode: false,
            ..AppConfig::default()
        };
        assert!(save_to(&p, &cfg));
        let loaded = load_from(&p);
        assert!(!loaded.update_enabled);
        assert_eq!(loaded.update_feed_url, "https://example.com/aurora/latest.json");
        assert!(loaded.wallpaper_multi_monitor);
        assert!(!loaded.wallpaper_span_mode);
        let _ = std::fs::remove_file(&p);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `export PATH="$PATH:$HOME/.cargo/bin" && cargo test -p aurora-desktop --lib missing_phase5_fields_fall_back_per_field` 等两个测试
Expected: 编译失败/断言失败(字段不存在)

- [ ] **Step 3: 实现字段**

`AppConfig` 结构体 `theme_accent` 行后追加(带注释分组):

```rust
    // ---- Phase5 5.1 自动更新(设计文档 §1)----
    pub update_enabled: bool,        // 总开关,默认 true;false 不检查不提示
    pub update_feed_url: String,     // 更新源 latest.json 地址,默认 GitHub 仓库内维护文件
    // ---- Phase5 5.2 多屏壁纸(设计文档 §2)----
    pub wallpaper_multi_monitor: bool, // 多屏开关,默认 false(= 现状只铺主屏)
    pub wallpaper_span_mode: bool,     // true=拼接(一张素材铺满虚拟桌面);false=每屏独立素材
```

`Default` impl 中 `theme_accent: "blue".to_string(),` 后追加:

```rust
            update_enabled: true,
            update_feed_url: "https://raw.githubusercontent.com/sssst1118/aurora-desktop/main/latest.json".to_string(),
            wallpaper_multi_monitor: false,
            wallpaper_span_mode: true,
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p aurora-desktop --lib config`
Expected: 全 PASS(含既有 config 测试,验证逐字段回退不破坏老行为)

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/commands/config.rs
git commit -m "配置新增 Phase5 四字段:更新开关与多屏模式"
```

---

### Task 2: 5.3 AI 工具扩展(纯函数层,最先交付)

**Files:**
- Modify: `src-tauri/src/ai/tools.rs`(ToolAction enum、route、rule_match、测试区)
- Modify: `src-tauri/src/commands/ai.rs`(exec_tool_action 两个 match 分支)

**Interfaces:**
- Consumes: 现有 `ToolAction`、`route()`、`rule_match()`、`tools_json()`;4.1 命令层 `wallpaper_dynamic_set(app: AppHandle, path: String) -> Result<WallpaperSetInfo, String>` 与 `wallpaper_dynamic_clear(app: AppHandle) -> Result<(), String>`(commands/wallpaper_dynamic.rs,签名不变)
- Produces: `ToolAction::SetDynamicWallpaper { path: String, url: Option<String> }`、`ToolAction::StopDynamicWallpaper`;route/rule_match 对该两变体的解析分支(commands/ai.rs 消费)

- [ ] **Step 1: 写失败测试**(在 tools.rs 测试区追加)

```rust
    // ---- Phase5 5.3 动态壁纸工具(设计文档 §3)----

    #[test]
    fn route_set_dynamic_wallpaper_requires_path_or_url() {
        // 缺全部必填 → Err
        assert!(route("set_dynamic_wallpaper", &json!({})).is_err());
        // 相对路径 → Err
        assert!(route("set_dynamic_wallpaper", &json!({"file_path": "videos\\a.mp4"})).is_err());
        // 合法:绝对路径
        let ok = route("set_dynamic_wallpaper", &json!({"file_path": r"C:\mat\a.mp4"}));
        assert_eq!(ok, Ok(ToolAction::SetDynamicWallpaper { path: r"C:\mat\a.mp4".to_string(), url: None }));
        // 合法:url 网页素材
        let ok2 = route("set_dynamic_wallpaper", &json!({"url": "https://example.com/bg.html"}));
        assert_eq!(ok2, Ok(ToolAction::SetDynamicWallpaper { path: String::new(), url: Some("https://example.com/bg.html".to_string()) }));
        // file_path 与 url 同时给 → 接受(file_path 优先)
        let ok3 = route("set_dynamic_wallpaper", &json!({"file_path": r"C:\mat\a.mp4", "url": "https://x.com/y.html"}));
        assert!(matches!(ok3, Ok(ToolAction::SetDynamicWallpaper { path, url }) if path == r"C:\mat\a.mp4" && url.as_deref() == Some("https://x.com/y.html")));
    }

    #[test]
    fn route_stop_dynamic_wallpaper_no_args() {
        assert!(route("stop_dynamic_wallpaper", &json!({})).is_ok());
        assert!(route("stop_dynamic_wallpaper", &json!({"extra": 1})).is_err());
    }

    #[test]
    fn rule_match_set_wallpaper_keywords() {
        assert!(matches!(rule_match("把这个视频设为壁纸"), Some(ToolAction::SetDynamicWallpaper { .. })));
        assert!(matches!(rule_match("换成壁纸"), Some(ToolAction::SetDynamicWallpaper { .. })));
        assert!(matches!(rule_match("设置壁纸"), Some(ToolAction::SetDynamicWallpaper { .. })));
    }

    #[test]
    fn rule_match_stop_wallpaper_keywords() {
        assert!(matches!(rule_match("停止壁纸"), Some(ToolAction::StopDynamicWallpaper)));
        assert!(matches!(rule_match("关闭壁纸"), Some(ToolAction::StopDynamicWallpaper)));
        // 组合词子串回归(3.2 老坑):"设置壁纸"不能误匹配 stop
        assert!(matches!(rule_match("设置壁纸"), Some(ToolAction::SetDynamicWallpaper { .. })));
    }

    #[test]
    fn tools_json_contains_dynamic_wallpaper_tools() {
        let arr = tools_json().as_array().unwrap();
        let names: Vec<&str> = arr.iter()
            .filter_map(|t| t["function"]["name"].as_str())
            .collect();
        assert!(names.contains(&"set_dynamic_wallpaper"));
        assert!(names.contains(&"stop_dynamic_wallpaper"));
    }
```

注意:tools.rs 测试区须引用 `use serde_json::json;`(若测试区未引入,顶部测试 mod 内加)。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p aurora-desktop --lib tools`
Expected: 编译失败(新变体不存在)

- [ ] **Step 3: 实现 ToolAction + route**

`ToolAction` enum 追加两个变体:

```rust
    SetDynamicWallpaper { path: String, url: Option<String> },
    StopDynamicWallpaper,
```

`route()` 的 `"get_clipboard_history"` 分支后追加两个分支:

```rust
        "set_dynamic_wallpaper" => {
            let path = optional_str(args, "file_path")?.unwrap_or_default();
            let url = optional_str(args, "url")?;
            if path.is_empty() && url.is_none() {
                return Err("set_dynamic_wallpaper 需要 file_path 或 url 至少一个".to_string());
            }
            if !path.is_empty() {
                require_abs(&path, "file_path")?;
            }
            Ok(ToolAction::SetDynamicWallpaper { path, url })
        }
        "stop_dynamic_wallpaper" => {
            ensure_no_args(args)?;
            Ok(ToolAction::StopDynamicWallpaper)
        }
```

需确认 tools.rs 现有辅助函数 `optional_str(args, key) -> Result<Option<String>, String>` 存在(3.2 已有 optional_str_array;若 `optional_str` 不存在,先加该辅助函数:取值 `args.get(key).and_then(|v| v.as_str()).map(String::from)`,key 缺失 → Ok(None),类型错误 → Err)。查现有实现为准,不重复定义。

- [ ] **Step 4: 实现 rule_match**

`rule_match` 现有逻辑(OPEN_KW → FIND_KW)之后、返回 None 之前,追加壁纸关键词块(命中删除该类全部词后返回动作;剩余词仅在设壁纸时需要——作目录内素材匹配提示,先不消费,留 None 语义也可,但保持与现有风格:返回动作即可):

```rust
    // ---- Phase5 5.3:壁纸关键词(长度降序,宁缺勿误匹配;设计文档 §3.2 保守词表)----
    const SET_WALLPAPER_KW: [&str; 6] = ["设置壁纸", "换成壁纸", "设为壁纸", "改成壁纸", "换壁纸", "设壁纸"];
    const STOP_WALLPAPER_KW: [&str; 4] = ["停止壁纸", "关掉壁纸", "关闭壁纸", "停壁纸"];

    for kw in STOP_WALLPAPER_KW {
        if instruction.contains(kw) {
            return Some(ToolAction::StopDynamicWallpaper);
        }
    }
    for kw in SET_WALLPAPER_KW {
        if instruction.contains(kw) {
            return Some(ToolAction::SetDynamicWallpaper { path: String::new(), url: None });
        }
    }
```

注意:stop 检查在 set 之前(防"设置壁纸"含"设"字干扰——现有词表无单字,顺序仍按 stop 先判,语义安全)。

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p aurora-desktop --lib tools`
Expected: 全 PASS(含既有 tools 测试)

- [ ] **Step 6: 实现 exec 分支**

`commands/ai.rs` 的 `exec_tool_action` 中 `ToolAction::GetClipboardHistory =>` 分支后追加:

```rust
        ToolAction::SetDynamicWallpaper { path, url } => {
            if let Some(u) = &url {
                if !u.starts_with("http://") && !u.starts_with("https://") {
                    return Err("动态壁纸 url 仅支持 http/https".to_string());
                }
            }
            if path.is_empty() && url.is_none() {
                return Err("缺少壁纸素材路径".to_string());
            }
            // 复用 4.1 命令层:图片走系统壁纸,视频/html 走 WorkerW;开关关闭时透传错误
            let info = crate::commands::wallpaper_dynamic::wallpaper_dynamic_set(app, path)?;
            Ok(("set_dynamic_wallpaper".to_string(), info.path))
        }
        ToolAction::StopDynamicWallpaper => {
            crate::commands::wallpaper_dynamic::wallpaper_dynamic_clear(app)?;
            Ok(("stop_dynamic_wallpaper".to_string(), "已恢复系统壁纸".to_string()))
        }
```

url 单独指定时(set 命令只收 path):url 非空且 path 为空 → 报错"网页素材请先下载到本地再设置"(4.1 set 契约无 url 参数;url 保留在 ToolAction 供未来扩展,当前执行不支持远端网页)。修正:exec 分支改为 path 为空(无论 url 是否给)→ Err("动态壁纸需要本地素材路径,网页素材请先下载到本地")。

- [ ] **Step 7: 全量测试 + 提交**

Run: `cargo test -p aurora-desktop`
Expected: 全绿(存量 + 新增)

```bash
git add src-tauri/src/ai/tools.rs src-tauri/src/commands/ai.rs
git commit -m "AI 新增动态壁纸工具:set/stop 白名单与关键词兜底"
```

---

### Task 3: 5.2 显示器枚举 + attach 坐标化(实现层)

**Files:**
- Modify: `src-tauri/src/wallpaper_dynamic.rs`(attach_to_workerw_at 新增、MonitorInfo 结构、monitor 枚举函数、测试区)

**Interfaces:**
- Consumes: 现有 `attach_to_workerw`(不动)、`find_workerw`;tauri `AppHandle::available_monitors()`
- Produces:
  - `pub struct MonitorInfo { pub index: u32, pub x: i32, pub y: i32, pub width: i32, pub height: i32, pub primary: bool }`
  - `pub fn enum_monitors(app: &AppHandle) -> Vec<MonitorInfo>`(虚拟桌面物理坐标,index = 排序后序号,主屏优先)
  - `pub fn attach_to_workerw_at(hwnd: HWND, x: i32, y: i32, width: i32, height: i32) -> Result<(), String>`(SetWindowPos 用 x/y;内部同 attach_to_workerw 三步)
  - `pub fn span_viewport(monitors: &[MonitorInfo]) -> (i32, i32, i32, i32)`(虚拟桌面整体 rect,纯函数可单测)

- [ ] **Step 1: 写失败测试**(测试区追加)

```rust
    // ---- Phase5 5.2 多屏(设计文档 §2):纯函数可单测,枚举/注入动作手动验收 ----

    #[test]
    fn span_viewport_computes_virtual_desktop_rect() {
        let mons = vec![
            MonitorInfo { index: 0, x: 0, y: 0, width: 1920, height: 1080, primary: true },
            MonitorInfo { index: 1, x: 1920, y: 0, width: 1280, height: 720, primary: false },
            MonitorInfo { index: 2, x: -1280, y: 200, width: 1280, height: 1024, primary: false },
        ];
        let (x, y, w, h) = span_viewport(&mons);
        // min_x=-1280, min_y=0, max_x=1920+1280=3200, max_y=max(1080, 720, 200+1024=1224)=1224
        assert_eq!((x, y, w, h), (-1280, 0, 4480, 1224));
    }

    #[test]
    fn span_viewport_empty_returns_default() {
        assert_eq!(span_viewport(&[]), (0, 0, 1920, 1080));
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p aurora-desktop --lib span_viewport`
Expected: 编译失败(结构/函数不存在)

- [ ] **Step 3: 实现 MonitorInfo + span_viewport + attach_to_workerw_at**

`attach_to_workerw` 函数后追加:

```rust
/// 显示器信息(虚拟桌面物理坐标;index = enum_monitors 排序后的序号,主屏恒为 0)
#[derive(Clone, Debug, PartialEq)]
pub struct MonitorInfo {
    pub index: u32,
    pub x: i32,
    pub y: i32,
    pub width: i32,
    pub height: i32,
    pub primary: bool,
}

/// 枚举全部显示器:tauri available_monitors(物理像素,免手写 FFI),
/// 排序 = 主屏优先 + 按 (x, y) 稳定排序,index 即排序序号。
/// 枚举失败(无桌面/异常)→ 返回仅主屏的兜底 1920x1080(与 attach 现状一致)
pub fn enum_monitors(app: &tauri::AppHandle) -> Vec<MonitorInfo> {
    let Ok(mons) = app.available_monitors() else {
        return vec![MonitorInfo { index: 0, x: 0, y: 0, width: 1920, height: 1080, primary: true }];
    };
    let mut out: Vec<MonitorInfo> = mons
        .iter()
        .map(|m| {
            let pos = m.position(); // PhysicalPosition<i32>,虚拟桌面坐标
            let size = m.size(); // PhysicalSize<u32>,物理像素
            MonitorInfo {
                index: 0,
                x: pos.x,
                y: pos.y,
                width: size.width as i32,
                height: size.height as i32,
                primary: m.is_primary(),
            }
        })
        .collect();
    // 主屏优先,其余按 x 升序(y 升序次)稳定排
    out.sort_by(|a, b| b.primary.cmp(&a.primary).then(a.x.cmp(&b.x)).then(a.y.cmp(&b.y)));
    for (i, m) in out.iter_mut().enumerate() {
        m.index = i as u32;
    }
    out
}

/// 虚拟桌面整体 rect(所有屏并集;纯函数):返回 (x, y, w, h);
/// 空列表 → (0, 0, 1920, 1080) 兜底
pub fn span_viewport(monitors: &[MonitorInfo]) -> (i32, i32, i32, i32) {
    if monitors.is_empty() {
        return (0, 0, 1920, 1080);
    }
    let min_x = monitors.iter().map(|m| m.x).min().unwrap_or(0);
    let min_y = monitors.iter().map(|m| m.y).min().unwrap_or(0);
    let max_x = monitors.iter().map(|m| m.x + m.width).max().unwrap_or(1920);
    let max_y = monitors.iter().map(|m| m.y + m.height).max().unwrap_or(1080);
    (min_x, min_y, max_x - min_x, max_y - min_y)
}
```

`attach_to_workerw` 下方追加(复用其三步,仅 SetWindowPos 坐标化):

```rust
/// 把 webview 窗口注入 WorkerW 并定位到虚拟桌面指定 rect(x/y 为虚拟桌面坐标)。
/// WorkerW 覆盖整个虚拟桌面,多屏场景每屏一个窗口、各自 SetWindowPos 到本屏 rect。
/// 注入失败返回错误,窗口保持原样
pub fn attach_to_workerw_at(hwnd: HWND, x: i32, y: i32, width: i32, height: i32) -> Result<(), String> {
    let progman = unsafe { FindWindowW(windows_sys::core::w!("Progman"), std::ptr::null()) };
    if progman.is_null() {
        return Err("未找到桌面窗口(Progman),请确认资源管理器运行中".to_string());
    }
    let workerw = find_workerw(progman)
        .ok_or_else(|| "WorkerW 壁纸层查找失败(桌面结构异常,建议重启资源管理器)".to_string())?;
    unsafe {
        SetParent(hwnd, workerw);
        SetWindowPos(hwnd, HWND_BOTTOM, x, y, width, height, SWP_NOACTIVATE | SWP_SHOWWINDOW);
    }
    Ok(())
}
```

文件顶部 import 区追加 `use tauri::Manager;`(available_monitors 在 Manager trait;若已有则跳过)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p aurora-desktop --lib wallpaper_dynamic`
Expected: 全 PASS(含既有测试)

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/wallpaper_dynamic.rs
git commit -m "多屏壁纸实现层:显示器枚举与坐标化注入"
```

---

### Task 4: 5.2 多屏命令层 + 接线

**Files:**
- Modify: `src-tauri/src/commands/wallpaper_dynamic.rs`(新命令 + set/clear 按模式分发 + 多屏状态)
- Modify: `src-tauri/src/lib.rs`(注册 3 新命令;setup 启动多屏重建线程)
- Modify: `src-tauri/src/commands/mod.rs`(无改动,命令已在 wallpapert_dynamic 模块内)

**Interfaces:**
- Consumes: Task 3 的 `enum_monitors` / `attach_to_workerw_at` / `span_viewport` / `MonitorInfo`;Task 1 的 `wallpaper_multi_monitor` / `wallpaper_span_mode`
- Produces: 命令 `wallpaper_multi_apply() -> Result<(), String>`、`wallpaper_multi_monitors() -> Vec<MonitorInfo>`、`wallpaper_dynamic_set_monitor(path, index) -> Result<WallpaperSetInfo, String>`(lib.rs invoke_handler 注册;前端 Task 5 消费)

- [ ] **Step 1: 实现多屏状态与命令**

`commands/wallpaper_dynamic.rs` 顶部追加(多屏内存状态,与 4.1 单值状态并存;多屏关时单值状态照旧):

```rust
/// 多屏模式下每屏素材状态(独立模式逐屏记录;拼接模式全部屏同一记录)
#[derive(Clone, Debug, serde::Serialize)]
pub struct PerMonitorState {
    pub index: u32,
    pub kind: String,
    pub path: String,
    pub url: Option<String>,
}

/// 每屏挂载的窗口 label 约定:主屏沿用 "wallpaper",副屏 "wallpaper_<index>"
pub fn monitor_window_label(index: u32) -> String {
    if index == 0 { "wallpaper".to_string() } else { format!("wallpaper_{index}") }
}
```

新命令实现(文件末尾追加;窗口创建用 WebviewWindowBuilder,tauri 2.11 方法名以本机 crate 源码为准,grep `WebviewWindowBuilder` 确认 builder 方法):

```rust
/// 枚举显示器(设置区展示 + 前端 span 切片计算)
#[tauri::command(rename = "wallpaper_multi_monitors")]
pub fn wallpaper_multi_monitors(app: AppHandle) -> Vec<crate::wallpaper_dynamic::MonitorInfo> {
    crate::wallpaper_dynamic::enum_monitors(&app)
}

/// 按当前配置重建多屏 attach:开关关 → 撤下全部副屏注入(主屏行为不变);
/// 开 → 逐屏创建/定位窗口并注入;拼接模式全部屏挂同一素材,独立模式挂各自素材
#[tauri::command(rename = "wallpaper_multi_apply")]
pub fn wallpaper_multi_apply(app: AppHandle) -> Result<(), String> {
    crate::wallpaper_dynamic::multi_apply(&app)
}

/// 独立模式:只给指定屏设置素材(越界/拼接模式报错)
#[tauri::command(rename = "wallpaper_dynamic_set_monitor")]
pub fn wallpaper_dynamic_set_monitor(app: AppHandle, path: String, index: u32) -> Result<WallpaperSetInfo, String> {
    let cfg = load_cfg(&app);
    if !cfg.wallpaper_multi_monitor {
        return Err("多屏壁纸未启用,请在设置中开启".to_string());
    }
    if cfg.wallpaper_span_mode {
        return Err("拼接模式下不支持单独设置某屏,请切换为独立模式".to_string());
    }
    let mons = crate::wallpaper_dynamic::enum_monitors(&app);
    if !mons.iter().any(|m| m.index == index) {
        return Err(format!("显示器 {index} 不存在(当前共 {} 台)", mons.len()));
    }
    // 校验 + 素材类型 + URL 两段式,复用单屏 set 的公共逻辑(抽取 wallpapeper_dynamic::resolve_material_url)
    crate::wallpaper_dynamic::validate_set_args(&path)?;
    let kind = crate::wallpaper_dynamic::material_kind(&path);
    if kind == "image" {
        return Err("图片素材走系统壁纸,不支持按屏设置,请使用视频/html".to_string());
    }
    let url = crate::wallpaper_dynamic::resolve_material_url(
        &path,
        &crate::wallpaper_dynamic::default_pictures_dir(),
    )?;
    crate::wallpaper_dynamic::set_monitor_state(index, WallpaperState { path: path.clone(), kind: kind.to_string(), url: url.clone() })?;
    crate::wallpaper_dynamic::apply_monitor(app.clone(), index)?;
    Ok(WallpaperSetInfo { path, url })
}
```

`wallpaper_dynamic_set` / `wallpaper_dynamic_clear` 追加多屏分支(多屏开:set 走全屏分发,clear 撤全部副屏;单屏路径不动):

```rust
    // wallpaper_dynamic_set 的 "video"|"html" 分支开头(校验配置后):
    let cfg_multi = load_cfg(&app);
    if cfg_multi.wallpaper_multi_monitor {
        // 拼接:全屏挂同一素材;独立:只设主屏(指定屏走 set_monitor 命令)
        let mons = crate::wallpaper_dynamic::enum_monitors(&app);
        let span = cfg_multi.wallpaper_span_mode;
        for m in &mons {
            let _ = crate::wallpaper_dynamic::set_monitor_state(m.index, WallpaperState { path: path.clone(), kind: kind.to_string(), url: url.clone() });
        }
        crate::wallpaper_dynamic::multi_apply(&app)?;
        return Ok(WallpaperSetInfo { path, url });
    }
```

- [ ] **Step 2: 实现实现层 multi_apply / apply_monitor / set_monitor_state**

`wallpaper_dynamic.rs`(实现层)追加(窗口创建/定位/注入集中于此,命令层只转发):

```rust
/// 每屏素材内存状态(index → 素材;多屏模式使用)
static MONITOR_STATES: OnceLock<Mutex<Vec<Option<WallpaperState>>>> = OnceLock::new();

pub fn set_monitor_state(index: u32, st: WallpaperState) -> Result<(), String> {
    let mut g = MONITOR_STATES.get_or_init(|| Mutex::new(Vec::new())).lock().map_err(|_| "状态锁失败".to_string())?;
    if g.len() <= index as usize { g.resize(index as usize + 1, None); }
    g[index as usize] = Some(st);
    Ok(())
}

pub fn monitor_states() -> Vec<Option<WallpaperState>> {
    MONITOR_STATES.get().map(|m| m.lock().ok().map(|g| g.clone()).unwrap_or_default()).unwrap_or_default()
}

/// 创建/复用指定屏的壁纸窗口并注入:窗口不存在 → WebviewWindowBuilder 创建
/// (label = wallpaper_<index>,加载 index.html 前端按 label 分流渲染);
/// 窗口存在 → 直接 set_size + show + attach
pub fn apply_monitor(app: &tauri::AppHandle, index: u32) -> Result<(), String> {
    use tauri::{WebviewUrl, WebviewWindowBuilder};
    let label = crate::commands::wallpaper_dynamic::monitor_window_label(index);
    let mons = enum_monitors(app);
    let Some(mon) = mons.iter().find(|m| m.index == index) else {
        return Err(format!("显示器 {index} 不存在"));
    };
    let win = if let Some(w) = app.get_webview_window(&label) {
        w
    } else {
        // 副屏窗口:与主屏同配置(无边框/不透明/不置顶/不可缩放/skipTaskbar/focus:false)
        WebviewWindowBuilder::new(app, &label, WebviewUrl::App("index.html".into()))
            .decorations(false)
            .transparent(false)
            .resizable(false)
            .skip_taskbar(true)
            .visible(false)
            .focused(false) // 防抢焦点(铁律);方法名以本机 tauri 2.11 crate 源码为准,
                            // 若不存在则创建后 tauri::async_runtime::block_on(win.set_focus(false))
            .build()
            .map_err(|e| format!("创建壁纸窗口 {label} 失败: {e}"))?
    };
    let hwnd = win.hwnd().map_err(|e| format!("获取壁纸窗口句柄失败: {e}"))?;
    let _ = win.set_size(tauri::PhysicalSize::new(mon.width as u32, mon.height as u32));
    let _ = win.set_position(tauri::PhysicalPosition::new(mon.x, mon.y));
    let _ = win.show();
    let _ = win.set_always_on_top(false);
    attach_to_workerw_at(hwnd.0 as *mut core::ffi::c_void, mon.x, mon.y, mon.width, mon.height)
}

/// 多屏整体重建(设置变更/热插拔后调用):
/// 多屏关 → 只保留主屏注入(现状),副屏窗口销毁/撤下;
/// 多屏开 → 全部屏按各自素材注入(无素材的屏不注入)
pub fn multi_apply(app: &tauri::AppHandle) -> Result<(), String> {
    let cfg = crate::commands::config::load_from(&crate::commands::config::config_path(app));
    let mons = enum_monitors(app);
    if !cfg.wallpaper_multi_monitor {
        // 撤下全部副屏窗口(1..n)
        for m in mons.iter().filter(|m| m.index > 0) {
            let label = crate::commands::wallpaper_dynamic::monitor_window_label(m.index);
            if let Some(w) = app.get_webview_window(&label) {
                if let Ok(hwnd) = w.hwnd() {
                    detach_from_workerw(hwnd.0 as *mut core::ffi::c_void);
                }
                let _ = w.destroy();
            }
        }
        return Ok(()); // 主屏保持现状(由 4.1 set/attach 路径管理)
    }
    let states = monitor_states();
    for m in &mons {
        let st = states.get(m.index as usize).and_then(|s| s.clone());
        if let Some(st) = st {
            if st.kind == "video" || st.kind == "html" {
                apply_monitor(app, m.index)?;
            }
        } else if m.index == 0 {
            // 主屏无记录:不注入(系统壁纸可见)
            let label = monitor_window_label(0);
            if let Some(w) = app.get_webview_window(&label) {
                if let Ok(hwnd) = w.hwnd() {
                    detach_from_workerw(hwnd.0 as *mut core::ffi::c_void);
                }
            }
        }
    }
    Ok(())
}
```

注意:`apply_monitor` 里副屏窗口素材渲染:窗口加载同一 index.html,前端按 window label 分流(Task 5);素材 URL 由前端从 get_state 扩展接口获取——本 Task 同时扩展 `wallpaper_dynamic_get_state`,返回值追加 `monitors: Vec<PerMonitorState>`(多屏关 → 空数组,前端保持旧逻辑)。

- [ ] **Step 3: 注册命令 + 接线 lib.rs**

`lib.rs` invoke_handler 的 `wallpaper_dynamic_get_state` 后追加注册:
`crate::commands::wallpaper_dynamic::wallpaper_multi_apply, crate::commands::wallpaper_dynamic::wallpaper_multi_monitors, crate::commands::wallpaper_dynamic::wallpaper_dynamic_set_monitor,`

setup 内追加热插拔重建线程(2s 轮询布局签名,变化 → 调用 multi_apply;开关关时不启动):

```rust
            // 5.2 多屏热插拔:2s 轮询显示器布局,变化时重建(轻量枚举,与 2.5 采样线程同风格)
            if crate::commands::config::load_from(&crate::commands::config::config_path(&handle)).wallpaper_multi_monitor {
                let mon_app = handle.clone();
                std::thread::Builder::new()
                    .name("monitor-watcher".to_string())
                    .spawn(move || {
                        let mut last: Vec<(i32, i32, i32, i32)> = Vec::new();
                        loop {
                            std::thread::sleep(std::time::Duration::from_secs(2));
                            let mons = crate::wallpaper_dynamic::enum_monitors(&mon_app);
                            let sig: Vec<(i32, i32, i32, i32)> = mons.iter().map(|m| (m.x, m.y, m.width, m.height)).collect();
                            if sig != last {
                                last = sig;
                                let _ = crate::wallpaper_dynamic::multi_apply(&mon_app);
                            }
                        }
                    })
                    .ok();
            }
```

- [ ] **Step 4: 编译 + 测试**

Run: `export PATH="$PATH:$HOME/.cargo/bin" && cargo test -p aurora-desktop`
Expected: 全绿(新增代码编译通过;注入动作本身无单测,逻辑单测在 Task 3 已覆盖)

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/commands/wallpaper_dynamic.rs src-tauri/src/wallpaper_dynamic.rs src-tauri/src/lib.rs
git commit -m "多屏壁纸命令接线:逐屏注入与热插拔重建"
```

---

### Task 5: 5.2 前端(设置区多显示器小节 + 壁纸窗口分流渲染)

**Files:**
- Modify: `src/components/Settings.vue`(壁纸区块追加多显示器小节)
- Modify: `src/App.vue` 或现有 wallpaper 渲染组件(按 window label 分流:主屏旧逻辑;副屏 index 渲染)

**Interfaces:**
- Consumes: Task 4 命令 `wallpaper_multi_monitors` / `wallpaper_multi_apply` / `wallpaper_dynamic_set_monitor`;`wallpaper_dynamic_get_state` 扩展的 `monitors` 字段
- Produces: 前端可操作的多屏设置 UI;副屏窗口 span 切片渲染

- [ ] **Step 1: 定位现状,再改设置区 UI**

先读 `src/App.vue` 找 wallpaper 窗口分支(4.1 接线处)与 `src/components/Settings.vue` 壁纸区块(4.1 素材列表/设置按钮的现成模式),确认现有组件结构与 invoke 调用方式后,在壁纸区块内追加"多显示器"小节:
- 开关 `wallpaper_multi_monitor`(invoke `config_save` 后调 `wallpaper_multi_apply`)
- 模式单选(拼接 / 独立),`wallpaper_span_mode`
- 独立模式下:调用 `wallpaper_multi_monitors` 列出显示器(序号/主屏标记/分辨率),每屏一个素材选择(调用 `wallpaper_dynamic_set_monitor`)
- 只读展示枚举结果(显示器数量/布局)

具体模板与交互代码以现有 Settings.vue 壁纸区块风格实现(4.1 区块已有素材列表/设置按钮的现成模式,照抄其结构)。

- [ ] **Step 2: 壁纸窗口分流渲染**

壁纸窗口渲染组件(4.1 实现,位置以实际为准,src/ 下 wallpaer 相关组件)内:
- `getCurrentWindow().label` 读窗口 label;
- label == "wallpaper" → 现有渲染逻辑不变;
- label == "wallpaper_<i>" → 解析 i,调用 `wallpaper_dynamic_get_state` 取 `monitors[i]` 的 url/path:
  - 拼接模式:`?span=1` 时素材坐标系 = 虚拟桌面 span(从 `wallpaper_multi_monitors` 计算),窗口内 CSS 定位显示本屏切片(transform: translate(-minX, -minY) + 容器尺寸 = span 尺寸);
  - 独立模式:直接渲染本屏素材(url/path);
  - 无素材 → 空白(系统壁纸可见)

- [ ] **Step 3: 构建验证**

Run: `pnpm build`
Expected: 成功,无 TS 错误

- [ ] **Step 4: 提交**

```bash
git add src/
git commit -m "多屏壁纸前端:设置区多显示器小节与逐屏渲染"
```

---

### Task 6: 5.1 updater 实现层(纯逻辑)

**Files:**
- Create: `src-tauri/src/updater.rs`
- Modify: `src-tauri/Cargo.toml`(加 `sha2 = "0.10"`)
- Modify: `src-tauri/src/lib.rs`(mod updater 声明;由 Task 7 接线)

**Interfaces:**
- Consumes: 无(tauri 依赖仅 AppHandle 用于事件,纯函数不依赖)
- Produces:
  - `pub struct UpdateInfo { pub version: String, pub url: String, pub sha256: String, pub notes: Option<String> }`
  - `pub fn parse_update_info(json: &str) -> Option<UpdateInfo>`(缺 version/url/sha256 任一 → None)
  - `pub fn compare_versions(current: &str, latest: &str) -> bool`(latest > current 语义化比较;不可解析段按 0;返回是否"有更新")
  - `pub fn version_cmp(a: &str, b: &str) -> std::cmp::Ordering`(compare_versions 的内部语义,供 max_by 排序选最新版)
  - `pub fn sha256_hex(path: &Path) -> Result<String, String>`(读文件计算;sha2 crate)
  - `pub fn download_update(url: &str, dest: &Path) -> Result<(), String>`(reqwest GET 流式写文件;tokio 运行时)

- [ ] **Step 1: Cargo.toml 加依赖**

`reqwest` 行后追加:

```toml
# 5.1 自动更新:下载产物 SHA-256 校验(自研 updater,不引需签名的官方插件)
sha2 = "0.10"
```

- [ ] **Step 2: 写失败测试**(新建 updater.rs 测试区)

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_update_info_valid() {
        let j = r#"{"version":"0.2.0","url":"https://github.com/sssst1118/aurora-desktop/releases/download/v0.2.0/Aurora_0.2.0_x64-setup.exe","sha256":"0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef","notes":"修复若干问题"}"#;
        let info = parse_update_info(j).unwrap();
        assert_eq!(info.version, "0.2.0");
        assert!(info.url.ends_with("x64-setup.exe"));
        assert_eq!(info.notes.as_deref(), Some("修复若干问题"));
    }

    #[test]
    fn parse_update_info_missing_fields_rejected() {
        assert!(parse_update_info(r#"{"version":"0.2.0"}"#).is_none());
        assert!(parse_update_info("not json").is_none());
        assert!(parse_update_info(r#"{"version":"0.2.0","url":"x","sha256":""}"#).is_none());
    }

    #[test]
    fn compare_versions_semver() {
        assert!(compare_versions("0.1.0", "0.2.0"));
        assert!(compare_versions("0.1.9", "0.1.10"));
        assert!(compare_versions("0.9.0", "1.0.0"));
        assert!(!compare_versions("0.2.0", "0.2.0")); // 相等无更新
        assert!(!compare_versions("0.2.0", "0.1.0")); // 更旧无更新
        assert!(!compare_versions("bad", "0.2.0")); // 当前不可解析 → 视为无更新(0.0.0)
        assert!(!compare_versions("0.1.0", "bad")); // 最新不可解析 → 无更新
    }

    #[test]
    fn version_cmp_ordering_for_max() {
        use std::cmp::Ordering;
        assert_eq!(version_cmp("0.1.0", "0.2.0"), Ordering::Less);
        assert_eq!(version_cmp("0.2.0", "0.2.0"), Ordering::Equal);
        assert_eq!(version_cmp("1.0.0", "0.9.9"), Ordering::Greater);
        assert_eq!(version_cmp("0.1.0-beta", "0.1.0"), Ordering::Equal); // 预发布后缀忽略
    }

    #[test]
    fn sha256_hex_known_vector() {
        let p = std::env::temp_dir().join("aurora_sha_test.txt");
        std::fs::write(&p, b"abc").unwrap();
        // SHA-256("abc") = ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad
        assert_eq!(sha256_hex(&p).unwrap(), "ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad");
        let _ = std::fs::remove_file(&p);
    }

    #[test]
    fn sha256_hex_missing_file_err() {
        let p = std::env::temp_dir().join("aurora_sha_none.bin");
        assert!(sha256_hex(&p).is_err());
    }
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `export PATH="$PATH:$HOME/.cargo/bin" && cargo test -p aurora-desktop --lib updater`
Expected: 编译失败(模块不存在;先建空 updater.rs + lib.rs mod 声明)

- [ ] **Step 4: 实现纯函数**

```rust
//! 5.1 自动更新实现层(纯逻辑 + 下载;零 tauri-plugin 依赖,自研)
//!
//! - 更新源:远端 latest.json(GitHub Releases 或自建静态服务,地址可配置);
//! - 版本比较:自实现语义化(x.y.z 逐段,不可解析段按 0,忽略预发布后缀);
//! - 下载:SHA-256 校验(sha2 crate,防中间人;签名证书未采购前的替代防线);
//! - 安装:退出 app 前 spawn cmd 包装脚本(等安装器静默完成 → 启动新版本)。

use serde::Deserialize;

#[derive(Clone, Debug, Deserialize)]
pub struct UpdateInfo {
    pub version: String,
    pub url: String,
    pub sha256: String,
    #[serde(default)]
    pub notes: Option<String>,
}

/// 解析 latest.json;缺 version/url/sha256 任一或 JSON 非法 → None
pub fn parse_update_info(json: &str) -> Option<UpdateInfo> {
    let info: UpdateInfo = serde_json::from_str(json).ok()?;
    if info.version.trim().is_empty() || info.url.trim().is_empty() || info.sha256.trim().is_empty() {
        return None;
    }
    Some(info)
}

/// 语义化版本三段解析:x.y.z 逐段数值;段缺失按 0;不可解析段按 0;预发布后缀(-/+)忽略
fn version_parts(v: &str) -> [u32; 3] {
    let mut out = [0u32; 3];
    let mut it = v.split(['-', '+']).next().unwrap_or("").split('.');
    for slot in out.iter_mut() {
        *slot = it.next().and_then(|s| s.parse::<u32>().ok()).unwrap_or(0);
    }
    out
}

/// 语义化版本 Ordering(供 max_by 选最新版与比较)
pub fn version_cmp(a: &str, b: &str) -> std::cmp::Ordering {
    version_parts(a).cmp(&version_parts(b))
}

/// 语义化版本比较:latest > current → true(有更新);相等/更旧 → false
pub fn compare_versions(current: &str, latest: &str) -> bool {
    version_cmp(current, latest) == std::cmp::Ordering::Less
}

/// 文件 SHA-256 hex(小写)
pub fn sha256_hex(path: &std::path::Path) -> Result<String, String> {
    use sha2::{Digest, Sha256};
    let bytes = std::fs::read(path).map_err(|e| format!("读取文件失败: {e}"))?;
    let digest = Sha256::digest(&bytes);
    Ok(digest.iter().map(|b| format!("{b:02x}")).collect())
}

/// 下载到 dest(5 分钟总超时;请求失败/写失败 → Err;调用方负责 sha256 校验与清理)
pub fn download_update(url: &str, dest: &std::path::Path) -> Result<(), String> {
    let rt = tokio::runtime::Builder::new_current_thread()
        .enable_all()
        .build()
        .map_err(|e| format!("创建运行时失败: {e}"))?;
    rt.block_on(async {
        let client = reqwest::Client::builder()
            .timeout(std::time::Duration::from_secs(300))
            .build()
            .map_err(|e| format!("创建请求客户端失败: {e}"))?;
        let resp = client.get(url).send().await.map_err(|e| format!("下载失败: {e}"))?;
        if !resp.status().is_success() {
            return Err(format!("下载失败: HTTP {}", resp.status()));
        }
        let bytes = resp.bytes().await.map_err(|e| format!("读取响应失败: {e}"))?;
        std::fs::write(dest, &bytes).map_err(|e| format!("写入文件失败: {e}"))
    })
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p aurora-desktop --lib updater`
Expected: 全 PASS

- [ ] **Step 6: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/updater.rs src-tauri/src/lib.rs
git commit -m "自动更新实现层:版本比较与下载校验"
```

---

### Task 7: 5.1 updater 命令层 + 接线 + 前端

**Files:**
- Create: `src-tauri/src/commands/updater_cmd.rs`
- Modify: `src-tauri/src/commands/mod.rs`(声明 updater_cmd)
- Modify: `src-tauri/src/lib.rs`(注册 4 命令;setup 启动定时检查线程)
- Modify: `src-tauri/src/tray.rs`(托盘菜单加"检查更新")
- Modify: `src/components/Settings.vue`(更新区块)

**Interfaces:**
- Consumes: Task 6 全部函数;Task 1 的 `update_enabled` / `update_feed_url`
- Produces: 命令 `update_check` / `update_download` / `update_install` / `update_open_folder`;事件 `update-progress` / `update-downloaded` / `update-error`

- [ ] **Step 1: 命令层**

```rust
//! 5.1 自动更新命令层(四命令 + 事件;实现层在 updater.rs)

use serde::Serialize;
use tauri::{AppHandle, Emitter};

use crate::updater::{self, UpdateInfo};

/// 当前应用版本(与 tauri.conf.json version 一致;编译期注入)
const APP_VERSION: &str = env!("CARGO_PKG_VERSION");

/// 下载目录:%LOCALAPPDATA%\Aurora\updates
fn updates_dir() -> std::path::PathBuf {
    let base = std::env::var_os("LOCALAPPDATA")
        .map(std::path::PathBuf::from)
        .unwrap_or_else(|| std::path::PathBuf::from(r"C:\Users\Public\AppData\Local"));
    base.join("Aurora").join("updates")
}

#[derive(Clone, Debug, Serialize)]
pub struct UpdateCheckResult {
    pub status: String,      // "latest" | "available" | "error"
    pub version: Option<String>,
    pub notes: Option<String>,
    pub error: Option<String>,
}

fn load_cfg(app: &AppHandle) -> crate::commands::config::AppConfig {
    crate::commands::config::load_from(&crate::commands::config::config_path(app))
}

/// 手动检查更新(状态语义:latest/available/error;网络失败静默返回 error 文案)
#[tauri::command(rename = "update_check")]
pub async fn update_check(app: AppHandle) -> UpdateCheckResult {
    let cfg = load_cfg(&app);
    if !cfg.update_enabled {
        return UpdateCheckResult { status: "error".to_string(), version: None, notes: None, error: Some("自动更新已关闭".to_string()) };
    }
    // 带 30s 总超时(网络不通时静默返回 error,不挂死)
    let client = reqwest::Client::builder()
        .timeout(std::time::Duration::from_secs(30))
        .build()
        .map_err(|e| e.to_string())
        .unwrap_or_else(|_| reqwest::Client::new());
    let body = match client.get(&cfg.update_feed_url).send().await {
        Ok(r) if r.status().is_success() => match r.text().await {
            Ok(t) => t,
            Err(e) => return UpdateCheckResult { status: "error".to_string(), version: None, notes: None, error: Some(format!("读取更新源失败: {e}")) },
        },
        Ok(r) => return UpdateCheckResult { status: "error".to_string(), version: None, notes: None, error: Some(format!("更新源响应异常: HTTP {}", r.status())) },
        Err(e) => return UpdateCheckResult { status: "error".to_string(), version: None, notes: None, error: Some(format!("检查更新失败: {e}")) },
    };
    let Some(info) = updater::parse_update_info(&body) else {
        return UpdateCheckResult { status: "error".to_string(), version: None, notes: None, error: Some("更新源格式不合法".to_string()) };
    };
    if updater::compare_versions(APP_VERSION, &info.version) {
        UpdateCheckResult { status: "available".to_string(), version: Some(info.version), notes: info.notes, error: None }
    } else {
        UpdateCheckResult { status: "latest".to_string(), version: Some(info.version), notes: None, error: None }
    }
}

/// 下载新版安装包(需先 check 到 available);进度经 update-progress 事件;完成 emit update-downloaded
#[tauri::command(rename = "update_download")]
pub async fn update_download(app: AppHandle) -> Result<(), String> {
    let cfg = load_cfg(&app);
    if !cfg.update_enabled {
        return Err("自动更新已关闭".to_string());
    }
    let client = reqwest::Client::builder()
        .timeout(std::time::Duration::from_secs(30))
        .build()
        .map_err(|e| format!("创建请求客户端失败: {e}"))?;
    let body = client.get(&cfg.update_feed_url).send().await
        .map_err(|e| format!("检查更新失败: {e}"))?
        .text().await.map_err(|e| format!("读取更新源失败: {e}"))?;
    let info = updater::parse_update_info(&body).ok_or("更新源格式不合法")?;
    if !updater::compare_versions(APP_VERSION, &info.version) {
        return Err("当前已是最新版本".to_string());
    }
    let dir = updates_dir();
    std::fs::create_dir_all(&dir).map_err(|e| format!("创建更新目录失败: {e}"))?;
    let dest = dir.join(format!("Aurora_setup_{}.exe", info.version));
    // 已下载且校验通过 → 直接完成(幂等)
    if dest.exists() && updater::sha256_hex(&dest).ok().as_deref() == Some(info.sha256.as_str()) {
        let _ = app.emit("update-downloaded", &UpdateInfo { version: info.version, url: info.url, sha256: info.sha256, notes: info.notes });
        return Ok(());
    }
    updater::download_update(&info.url, &dest).map_err(|e| {
        let _ = app.emit("update-error", &serde_json::json!({ "message": e }));
        e
    })?;
    let actual = updater::sha256_hex(&dest).map_err(|e| format!("校验失败: {e}"))?;
    if actual != info.sha256 {
        let _ = std::fs::remove_file(&dest);
        return Err("下载文件校验失败(哈希不匹配),已删除,请重试".to_string());
    }
    let _ = app.emit("update-downloaded", &UpdateInfo { version: info.version, url: info.url, sha256: info.sha256, notes: info.notes });
    Ok(())
}

/// 退出并静默安装:spawn cmd 包装脚本(等安装器完成 → 启动新版本),然后退出 app
#[tauri::command(rename = "update_install")]
pub fn update_install(app: AppHandle) -> Result<(), String> {
    // 扫描 updates 目录取最新 version 安装包(与 update_download 产物对齐,无网络依赖)
    let dir = updates_dir();
    let mut candidates: Vec<(String, std::path::PathBuf)> = std::fs::read_dir(&dir)
        .map_err(|e| format!("读取更新目录失败: {e}"))?
        .flatten()
        .filter_map(|f| f.path().extension().map(|e| e.to_string_lossy().to_lowercase() == "exe").unwrap_or(false).then_some(f.path()))
        .filter_map(|p| {
            let name = p.file_name()?.to_string_lossy().into_owned();
            let v = name.strip_prefix("Aurora_setup_")?.strip_suffix(".exe")?.to_string();
            Some((v, p))
        })
        .collect();
    // 取语义化最大版本(version_cmp 为纯函数,见 updater.rs 单测)
    let Some((ver, exe)) = candidates
        .into_iter()
        .max_by(|a, b| updater::version_cmp(&a.0, &b.0))
    else {
        return Err("未找到已下载的安装包,请先下载更新".to_string());
    };
    let _ = ver;
    let install_dir = std::env::var_os("LOCALAPPDATA").map(std::path::PathBuf::from)
        .unwrap_or_else(|| std::path::PathBuf::from(r"C:\Users\Public\AppData\Local"))
        .join("Aurora");
    let app_exe = install_dir.join("Aurora.exe");
    // 包装脚本:等静默安装完成 → 启动新版本(独立 cmd 进程,与 app 生命周期解耦)
    let script = format!(
        "start /wait \"\" \"{}\" /S && start \"\" \"{}\"",
        exe.display(),
        app_exe.display()
    );
    let _ = std::process::Command::new("cmd")
        .args(["/C", &script])
        .creation_flags(0x08000000) // CREATE_NO_WINDOW
        .spawn()
        .map_err(|e| format!("启动安装器失败: {e}"))?;
    app.exit(0);
    Ok(())
}

/// 打开更新下载目录(手动安装兜底)
#[tauri::command(rename = "update_open_folder")]
pub fn update_open_folder() -> Result<(), String> {
    let dir = updates_dir();
    let _ = std::fs::create_dir_all(&dir);
    opener::open(&dir).map_err(|e| format!("打开目录失败: {e}"))
}
```

注意:`update_install` 中取最新版本的排序简化写法可能不正确,实现时用 `candidates.sort_by(|a, b| compare_versions(&b.0, &a.0).then(a.0.cmp(&b.0)))` 或改为显式 `max_by` 取最大版本(以正确语义为准,测试补一条纯函数 `pick_latest_candidate(Vec<(String, PathBuf)>)`)。

- [ ] **Step 2: 接线**

`lib.rs`:
- `mod updater;` 声明;
- `commands/mod.rs` 声明 `pub mod updater_cmd;`;
- invoke_handler 追加 4 命令;
- setup 追加定时检查线程(启动延迟 15s,每 6h;`update_enabled` 关时不启动;发现新版 emit `update-available`):

```rust
            // 5.1 自动更新:启动 15s 后 + 每 6h 静默检查;发现新版 emit update-available
            if crate::commands::config::load_from(&crate::commands::config::config_path(&handle)).update_enabled {
                let upd_app = handle.clone();
                std::thread::Builder::new()
                    .name("updater-watch".to_string())
                    .spawn(move || {
                        std::thread::sleep(std::time::Duration::from_secs(15));
                        loop {
                            // 注意:app2 由 async 块独占(move),emit 也在块内用 app2,
                            // 外层循环只持有 upd_app 用于 clone,避免 use-after-move
                            let app2 = upd_app.clone();
                            tauri::async_runtime::spawn(async move {
                                let r = crate::commands::updater_cmd::update_check(app2.clone()).await;
                                if r.status == "available" {
                                    let _ = app2.emit("update-available", &serde_json::json!({ "version": r.version, "notes": r.notes }));
                                }
                            });
                            std::thread::sleep(std::time::Duration::from_secs(6 * 3600));
                        }
                    })
                    .ok();
            }
```

注意:闭包内 `update_check` 是 async 命令函数,直接调用需返回 future;上面写法在 spawn 的 async 块内 `.await` 调用,签名对齐。

- [ ] **Step 3: 托盘菜单**

`tray.rs` 菜单追加"检查更新"项(现有菜单结构照抄,菜单点击 handler 调 `update_check` 异步命令,结果 available → 弹提示窗或 emit 给前端):

```rust
    // 菜单项(追加在现有"退出"前)
    let check_update = MenuItem::with_id(app, "check_update", "检查更新", true, None::<&str>)?;
```

- [ ] **Step 4: 前端设置页更新区块**

`Settings.vue` 追加"更新"区块:当前版本号(从 `tauri::getVersion` 或硬编码与 Cargo 一致)、"检查更新"按钮(调 `update_check`)、状态文案(latest/available/error)、available 时显示"下载并安装"按钮(串行调 `update_download` → `update_install`)、"打开下载目录"链接。监听 `update-available` / `update-progress` / `update-downloaded` / `update-error` 事件展示进度与结果。

- [ ] **Step 5: 编译 + 测试 + 构建**

Run: `cargo test -p aurora-desktop` + `pnpm build`
Expected: 全绿 + 构建成功

- [ ] **Step 6: 提交**

```bash
git add src-tauri/ src/
git commit -m "自动更新接线:命令注册定时检查与前端更新区块"
```

---

### Task 8: 5.1 NSIS 打包配置与构建验证

**Files:**
- Modify: `src-tauri/tauri.conf.json`(bundle.targets 加 nsis;bundle.nsis 配置)

**Interfaces:**
- Consumes: 无
- Produces: `Aurora_<version>_x64-setup.exe` 构建产物(updater 安装链路依赖其 /S 静默参数)

- [ ] **Step 1: 配置**

`tauri.conf.json` 修改:

```json
  "bundle": {
    "active": true,
    "targets": ["msi", "nsis"],
    "icon": ["icons/icon.ico"],
    "publisher": "Aurora Desktop",
    "copyright": "Copyright © 2026 sssst1118",
    "category": "Utility",
    "shortDescription": "Aurora - AI 增强模块化桌面效率中心",
    "longDescription": "灵动岛 / Dock / 全局搜索 / 文件抽屉 / 剪贴板历史 / 壁纸引擎 / AI 助手",
    "nsis": {
      "languages": ["SimpChinese"],
      "installMode": "currentUser"
    }
  }
```

先 grep 本机 tauri-bundler 源码确认 nsis 配置键名与默认值(`~/.cargo/registry/src/*/tauri-bundler-*/src/bundle/nsis/` 下 schema 或模板),SimpChinese 语言文件若模板不支持,回退只保留默认并记录错误台账。

- [ ] **Step 2: 构建验证**

Run: `export PATH="$PATH:$HOME/.cargo/bin" && export HTTPS_PROXY=http://127.0.0.1:7897 && export HTTP_PROXY=http://127.0.0.1:7897 && pnpm tauri build`
Expected: 产出 `src-tauri/target/release/bundle/nsis/Aurora_0.1.0_x64-setup.exe`(与 MSI 同目录并存);NSIS 工具下载走代理成功

- [ ] **Step 3: 验证静默安装参数**

Run: 手动安装验证 `/S` 静默参数(装到临时目录或实测安装;记录结果到手动验收清单)。本步骤如无 GUI 条件则标记待真机验收,不虚报。

- [ ] **Step 4: 提交**

```bash
git add src-tauri/tauri.conf.json
git commit -m "NSIS 打包:中文安装向导与静默安装支持"
```

---

### Task 9: 5.4 收口:dead_code 警告清理

**Files:**
- Modify: 按 `cargo build` 输出定位的 6 处警告文件

**Interfaces:**
- Consumes: 无
- Produces: 全树 0 警告(行为不变)

- [ ] **Step 1: 定位警告**

Run: `export PATH="$PATH:$HOME/.cargo/bin" && cargo build 2>&1 | grep -B2 "never used" | head -40`
Expected: 6 处警告(Phase1/2/3 历史)

- [ ] **Step 2: 逐处处理(每处:判断 → 删/接线/最小 allow → 跑测试)**

处理规则:
1. 已无用代码 → 删除;
2. 被接线遗漏的 pub 函数 → 检查是否有调用方应接线(补引用);
3. 确需保留(如为后续预留的契约函数)→ 加最小范围 `#[allow(dead_code)]` 并注释理由;
4. 每处修改后跑 `cargo test -p aurora-desktop` 防回归。

- [ ] **Step 3: 全量验证 + 提交**

Run: `cargo build 2>&1 | grep -c "warning"`(0)+ `cargo test -p aurora-desktop`(全绿)+ `pnpm build`

```bash
git add src-tauri/src/
git commit -m "清理历史 dead_code 警告,全树构建零警告"
```

---

### Task 10: 集成收尾

**Files:**
- Modify: `docs/开发进度.md`(看板:Phase5 状态/任务行/Next Actions)
- Modify: `README.md`(功能清单追加 Phase5 三项)
- Modify: `docs/错误记录.md`(本次新踩坑,如有)

**Interfaces:**
- Consumes: Task 1~9 全部
- Produces: 可发布版本(MSI + NSIS 双产物)

- [ ] **Step 1: 全量回归**

Run: `cargo test -p aurora-desktop`(全绿)+ `pnpm build` + release 构建 + 空闲内存复测(启动后 10s/20s/30s 采样,WS <120MB)
Expected: 全绿 + 达标

- [ ] **Step 2: 文档收口**

`开发进度.md`:Phase5 三块标 ✅(代码完成);Next Actions 追加真机验收项(双屏/NSIS 安装/自动更新全链路,清单引用本文件 §7);README 功能清单同步。

新建 `docs/代码签名接入.md`(设计文档 §1.4 的待办文档):证书采购后三步接入——① 获得代码签名证书(pfx 或证书存储);② 配置签名(以本机 tauri 2 配置 schema 实测为准,`bundle.signtool` 或构建后 signtool 命令);③ 构建后验证(`signtool verify`)。同时写明:签名缺失期间自研更新链路以 SHA-256 校验兜底,签名就绪后升级为签名验证。

- [ ] **Step 3: 提交 + 推送**

```bash
git add docs/ README.md
git commit -m "Phase5 集成收尾:回归全绿与文档同步"
git push origin main
# 失败则:git -c http.proxy= -c https.proxy= push origin main
```

- [ ] **Step 4: 状态通报**

向用户汇报:三块完成情况、测试结果、产物路径、待真机验收项清单(双屏拼接/独立、NSIS 安装升级卸载、自动更新全链路、静默安装)。
