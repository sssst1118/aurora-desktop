# Phase6 UI 重构实施计划(一岛一窗)

> **For agentic workers:** REQUIRED SUB-SKILL: 按任务用独立 agent 实施,每任务完成跑对应验证并 commit(中文 message)。步骤用 `- [ ]` 跟踪。
> 执行波次:**波1 = Task 1/2 并行 → 波2 = Task 3/4 并行(依赖波1) → 波3 = Task 5 → Task 6(集成收尾)**。
> 任务间接口以本文「Interfaces」为准;改动冲突时以设计文档 [../Phase6-UI重构-设计.md](../Phase6-UI重构-设计.md) 为准。

**Goal:** 把 6 窗口重构为「岛 + 主面板」一岛一窗形态,4 套皮肤+极光缘视觉系统。

**Architecture:** 窗口收敛为 island(药丸,展开括入 Dock)+ search(主面板,五视图合一,位置跟随岛);前端 main.ts/App.vue 分流相应收敛;视觉走 CSS 令牌皮肤包。

**Tech Stack:** Tauri2 + Rust 1.97 + Vue3-TS + Tailwind v3 + pnpm

**Spec:** docs/Phase6-UI重构-设计.md;设计预览稿 d:\2605\aurora-v02-preview.html(交互与视觉的参照实现,代码可直接移植)

## Global Constraints

- 质量门每任务:对应 `cargo test` 全绿 + `pnpm build` 通过;Task 6 全量 `cargo test`(基线 325 条)+ `pnpm tauri build`
- 内存目标:release 空闲 WS/PRIV < 120MB(Task 6 复测)
- 配置兼容:AppConfig 新字段必须 `#[serde(default)]`,老 config.json 缺失字段逐字段回退(铁律)
- commit message 中文,风格"第一行≤50字,正文写为什么"
- 皮肤字段 `skin`: `"deep"|"midnight"|"dawn"|"verdant"`,默认 "deep";强调色 4 色:青 #38bdf8/紫 #a78bfa/绿 #34d399/琥珀 #fbbf24
- 旧窗口 label(drawer/clipboard/ai_panel)删除后,其**数据命令层全部保留**(dock_*/drawer_*/clipboard_*/ai_* 命令),只改窗口层与前端挂载

---

### Task 1: 后端骨架(窗口收敛+岛药丸+配置+热键重定向)

**Files:**
- Modify: `src-tauri/tauri.conf.json`
- Modify: `src-tauri/src/commands/config.rs`(AppConfig 字段+island_save_geometry+单测)
- Modify: `src-tauri/src/win_utils.rs`(hide_all LABELS/show_search_window 定位岛下方/show_panel_with_view)
- Modify: `src-tauri/src/hotkey.rs`(三热键 handler 重定向)
- Modify: `src-tauri/src/lib.rs`(setup 清理+岛几何恢复+注册新命令)
- Modify: `src-tauri/src/runtime.rs`(清理对已删窗口的引用,先 grep 确认)
- Modify: `src-tauri/src/tray.rs`(托盘入口改为呼出主面板对应视图,先读文件确认现状)
- Modify: `src-tauri/capabilities/default.json`(补 window:allow-set-position 等,先读确认缺口)

**Interfaces:**
- Produces(前端 Task 3/4 消费):
  - 窗口: `island` 378×46(可拖,不 resizable,alwaysOnTop,skipTaskbar,focus:false,transparent);`search` 680×520(主面板,resizable,alwaysOnTop);`wallpaper` 不变;**drawer/clipboard/ai_panel 窗口删除**
  - 命令 `island_save_geometry(app, x: i32, y: i32) -> bool`(配置锁内读改写 island_x/island_y,不触发热生效)
  - 命令 `open_search`(已有,呼出主面板);`toggle_search`(已有)
  - 事件: 热键/托盘呼出对应视图时,后端 `app.emit("panel-open-view", { view: "drawer"|"clipboard"|"ai" })`(在 show 之后发,前端 MainPanel 监听切视图)
  - AppConfig 新字段: `skin: String`(默认 "deep")、`island_x: Option<i32>`、`island_y: Option<i32>`(默认 None=顶部居中)
- Consumes: 现有 `config_load/save_to/config_lock/clamp_to_visible/show_search_window`

- [ ] **Step 1: tauri.conf.json 窗口收敛**

island 改:width 378/height 46;search 改:width 680/height 520;删除 drawer/clipboard/ai_panel 三个窗口整块。其余字段不动。

- [ ] **Step 2: AppConfig 新字段+单测**

`src-tauri/src/commands/config.rs` AppConfig 结构体加:
```rust
    // ---- Phase6 UI 重构(2026-08-14,设计文档 §7)----
    pub skin: String,                 // 皮肤包 "deep"|"midnight"|"dawn"|"verdant",默认 "deep"
    pub island_x: Option<i32>,        // 灵动岛位置(逻辑像素);None=顶部居中
    pub island_y: Option<i32>,
```
Default 实现加 `skin: "deep".to_string(), island_x: None, island_y: None`。
单测(加在 tests mod 内,仿 `missing_stability_fields_fall_back_false`):
```rust
    #[test]
    fn missing_phase6_fields_fall_back_per_field() {
        let p = tmp_cfg("p6partial");
        std::fs::write(&p, r#"{"hotkey_search":"ctrl+alt+z"}"#).unwrap();
        let cfg = load_from(&p);
        assert_eq!(cfg.skin, "deep");
        assert_eq!(cfg.island_x, None);
        assert_eq!(cfg.island_y, None);
        let _ = std::fs::remove_file(&p);
    }
    #[test]
    fn phase6_fields_roundtrip() {
        let p = tmp_cfg("p6roundtrip");
        let cfg = AppConfig { skin: "dawn".to_string(), island_x: Some(120), island_y: Some(30), ..AppConfig::default() };
        assert!(save_to(&p, &cfg));
        let loaded = load_from(&p);
        assert_eq!(loaded.skin, "dawn");
        assert_eq!(loaded.island_x, Some(120));
        assert_eq!(loaded.island_y, Some(30));
        let _ = std::fs::remove_file(&p);
    }
```
跑 `cargo test commands::config` 验证 4 条新+旧全绿。

- [ ] **Step 3: island_save_geometry 命令**

config.rs 内仿 `search_save_geometry` 写(配置锁+不触发热生效):
```rust
/// 灵动岛拖动后记忆位置(前端 onMoved 防抖后调用;None 语义=居中,由前端判断)。
/// 只写配置文件、不触发热生效:位置是纯展示状态,下次启动由 setup 恢复。
#[tauri::command]
pub fn island_save_geometry(app: tauri::AppHandle, x: i32, y: i32) -> bool {
    let path = config_path(&app);
    let _guard = config_lock().lock().unwrap_or_else(|p| p.into_inner());
    let mut cfg = load_from(&path);
    cfg.island_x = Some(x);
    cfg.island_y = Some(y);
    save_to(&path, &cfg)
}
```

- [ ] **Step 4: win_utils 改造**

`hide_all` 的 LABELS 改为 `["island", "search"]`。
`show_search_window` 增加定位逻辑(在 show 之前):
```rust
pub fn show_search_window(app: &AppHandle) {
    if let Some(win) = app.get_webview_window("search") {
        // Phase6:主面板位置绑定岛——水平居中于岛,垂直在岛下方 12px;越界 clamp
        if let Some(island) = app.get_webview_window("island") {
            if let (Ok(ipos), Ok(isize)) = (island.outer_position(), island.outer_size()) {
                let scale = island.scale_factor().unwrap_or(1.0);
                let ix = ipos.x as f64 / scale;
                let iy = ipos.y as f64 / scale;
                let iw = isize.width as f64 / scale;
                let ih = isize.height as f64 / scale;
                if let Ok(ssize) = win.outer_size() {
                    let sscale = win.scale_factor().unwrap_or(1.0);
                    let w = ssize.width as f64 / sscale;
                    let h = ssize.height as f64 / sscale;
                    let monitors: Vec<(i32,i32,i32,i32)> = app.available_monitors().unwrap_or_default().iter().map(|m| { let s = m.scale_factor(); let p = m.position(); let sz = m.size(); ((p.x as f64/s).round() as i32,(p.y as f64/s).round() as i32,(sz.width as f64/s).round() as i32,(sz.height as f64/s).round() as i32)}).collect();
                    let px = (ix + iw/2.0 - w/2.0).round() as i32;
                    let py = (iy + ih + 12.0).round() as i32;
                    if let Some((cx, cy)) = crate::win_utils::clamp_to_visible(px, py, w as i32, h as i32, &monitors) {
                        let _ = win.set_position(tauri::LogicalPosition::new(cx, cy));
                    }
                }
            }
        }
        // ...原有 show/置顶/Alt 伪按键/SetForegroundWindow/set_focus 逻辑不变
    }
}
```
新增:
```rust
/// 呼出主面板并定位到指定视图(抽屉/剪贴板/AI 热键与托盘入口共用)。
/// 事件在 show 后 emit,前端 MainPanel 监听 "panel-open-view" 切换视图。
pub fn show_panel_with_view(app: &AppHandle, view: &str) {
    show_search_window(app);
    let _ = app.emit("panel-open-view", serde_json::json!({ "view": view }));
}
```

- [ ] **Step 5: hotkey.rs 三热键重定向**

`toggle_drawer_window` 改 `crate::win_utils::show_panel_with_view(app, "drawer")`;
`toggle_clipboard_window` 改 `..."clipboard"`;
`toggle_ai_panel_window` 改 `..."ai"`。删除 `toggle_window` 泛型函数(不再有独立窗口)与未用 import。`handler_for` 不变。

- [ ] **Step 6: lib.rs setup 改造**

删除 drawer_open_on_launch 分支(get_webview_window("drawer") 已不存在);search 几何恢复改为只恢复尺寸(删除 set_position 部分,位置由呼出时绑定岛);新增 island 几何恢复:
```rust
            // Phase6:灵动岛位置恢复(拖动记忆;越界回退顶部居中)
            if let Some(win) = app.get_webview_window("island") {
                if let (Some(x), Some(y)) = (cfg.island_x, cfg.island_y) {
                    let monitors: Vec<(i32,i32,i32,i32)> = app.available_monitors().unwrap_or_default().iter().map(|m| { let s = m.scale_factor(); let p = m.position(); let sz = m.size(); ((p.x as f64/s).round() as i32,(p.y as f64/s).round() as i32,(sz.width as f64/s).round() as i32,(sz.height as f64/s).round() as i32)}).collect();
                    if let Some((cx, cy)) = crate::win_utils::clamp_to_visible(x, y, 378, 46, &monitors) {
                        let _ = win.set_position(tauri::LogicalPosition::new(cx, cy));
                    }
                }
            }
```
invoke_handler 注册 `commands::config::island_save_geometry`。

- [ ] **Step 7: runtime.rs / tray.rs / first_run.rs 清理**

grep `"drawer"`/`"clipboard"`/`"ai_panel"` 的窗口 label 引用:runtime.rs 的窗口显隐逻辑、tray.rs 的托盘动作(改为 `show_panel_with_view`)、first_run.rs 若有引用一并改。drawer_open_on_launch 字段保留但不再消费(兼容)。

- [ ] **Step 8: capabilities/default.json 检查**

读文件确认含:core:window 的 allow-set-position/allow-set-size/allow-set-always-on-top/allow-start-dragging、core:event 的 allow-listen/allow-emit(前端 emit island-geometry 需要)。缺口补齐。

- [ ] **Step 9: 验证+commit**

`cargo test` 全绿(新增 2 条+旧全过);`cargo check` 无警告。commit: "Phase6 后端骨架:窗口收敛为岛+主面板,皮肤字段与热键重定向"

---

### Task 2: 视觉系统(皮肤令牌+极光缘+SVG 图标库)

**Files:**
- Modify: `src/styles/global.css`(重写:4 皮肤令牌+极光缘+通用面板类+字号圆角收敛)
- Modify: `src/theme.ts`(皮肤接线 apply_skin+兼容映射)
- Create: `src/components/icons/AuroraIcon.vue`(name prop 内联 SVG)
- Create: `src/components/icons/paths.ts`(SVG path 数据,从设计稿移植)

**Interfaces:**
- Produces:
  - 全局类 `.aurora-panel`(玻璃底+1px 边框+16px 圆角+极光缘::before 流动光带+::after 柔光;深/浅/4 皮肤全由令牌驱动)——Task 3/4 各窗口根容器加此类
  - `<AuroraIcon name="search|settings|clipboard|ai|drawer|close|copy|file|folder|grip|trash|plus|check" :size="15" />`(stroke=currentColor,stroke-width 1.7)
  - `apply_skin(skin: string)` 导出:设置 `document.documentElement.dataset.skin`;`apply_theme` 扩展为同时处理 skin 与 accent(4 新色)
  - 兼容映射:cfg.theme_mode === "light" 且 skin 为 "deep"(老用户升级)→ 按 "dawn" 呈现(theme.ts 内做,不改后端)
  - 全局:细窄滚动条(6px)/输入光标 accent/`:focus-visible` outline/`prefers-reduced-motion` 降级
- Consumes: 现有 `--aurora-*` 命名;设计稿 d:\2605\aurora-v02-preview.html 的 CSS 令牌值与 SVG path(直接移植,改 body 选择器为 `:root[data-skin=...]`)

- [ ] **Step 1: global.css 重写**

保留现有 `--aurora-*` 键名与语义色,`:root` 块=deep 皮肤值(从设计稿 body 默认块移植),新增 `:root[data-skin="midnight"]`/`":root[data-skin="dawn"]`/`:root[data-skin="verdant"]` 三块覆盖;新增 `--aur-1/--aur-2/--aur-3`(极光带三色)与 `--glass-hi` 令牌到每个皮肤块。新增 `.aurora-panel` 类(含 `::before` 极光缘+`::after` 柔光,动画 aurora-flow 9s)与全局滚动条/光标/焦点样式。**注意 dawn 皮肤下 `color-scheme: light`。**

- [ ] **Step 2: paths.ts + AuroraIcon.vue**

paths.ts 导出 `export const ICON_PATHS: Record<string,string>`(name → 完整 `<path>/<circle>` 等内联片段);AuroraIcon.vue:
```vue
<script setup lang="ts">
import { ICON_PATHS } from "./paths";
const props = withDefaults(defineProps<{ name: string; size?: number }>(), { size: 15 });
</script>
<template>
  <svg :width="size" :height="size" viewBox="0 0 24 24" fill="none"
    stroke="currentColor" stroke-width="1.7" stroke-linecap="round"
    stroke-linejoin="round" v-html="ICON_PATHS[name] || ''" aria-hidden="true" />
</template>
```
图标至少含:search/settings/clipboard/ai/drawer/close/copy/file/folder/grip/trash/plus/check(home 不需要)。

- [ ] **Step 3: theme.ts 皮肤接线**

ACCENT_COLORS 更新为 `{blue:"#38bdf8", purple:"#a78bfa", green:"#34d399", orange:"#fbbf24"}`;`apply_theme` 签名扩展为 `(cfg: {theme_mode:string; theme_accent:string; skin?:string})`,逻辑:skin 有效值 → `document.documentElement.dataset.skin = skin`;skin 缺失/默认 "deep" 且 theme_mode==="light" → dataset.skin="dawn"(兼容映射);其余 → "deep"。main.ts 的 invoke 返回类型加 `skin: string`。`applyDarkState` 保留(dawn 由令牌自身保证浅色,html.dark 仅影响残留 dark 变体)。

- [ ] **Step 4: 验证+commit**

`pnpm build` 通过;`cargo test` 不受影响(确认无回归)。commit: "Phase6 视觉系统:四皮肤令牌包+极光缘签名元素+SVG 图标库"

---

### Task 3: 灵动岛前端(三态+岛 Dock+拖动+几何)

**Files:**
- Modify: `src/components/core/Island.vue`(整文件重写,参照设计稿 .island 结构)
- Create: `src/components/core/IslandDock.vue`(岛内 Dock:图标瓦片/绿点/✕/＋/溢出)
- Modify: `src/components/core/Dock.vue`(退役:内容并入 IslandDock 后删除文件)

**Interfaces:**
- Consumes: 命令 `dock_get_items/dock_set_items/dock_launch/dock_get_running/dock_get_icon/island_save_geometry/open_search`;事件 `tauri://move`(onMoved);`getCurrentWindow().onDragDropEvent`;`emit("island-geometry", {x,y,w,h})`(逻辑像素,拖动防抖 600ms 后发)
- Produces: 事件 `island-geometry`(Task 4 主面板跟随消费);窗口行为:单击展开(窗口 rAF setSize 378→648 宽,~280ms)/双击 invoke open_search/展开态图标点击启动
- 关键行为契约(设计文档 §3):点击图标=启动(stopPropagation+LAUNCHING 防连点+脉冲+岛收回);拖动>4px 不触发单击;✕ 删除;拖入 .exe/.lnk 添加;溢出 >9 个显示「…」按钮浮层全列表

- [ ] **Step 1: Island.vue 重写(结构+展开动画)**

结构参照设计稿 `.island`(pulse-dot/time/stats + divider + IslandDock + search-entry 按钮);`data-tauri-drag-region` 放根容器(与现版一致,按钮自动豁免)。
展开/收起:调 `getCurrentWindow().setSize(new LogicalSize(648或378, 46))` 用 rAF 步进动画(仿 SearchBar resizeMove 的 rAF 节流模式,~280ms,每帧 4-6 步);内部 `.expanded` class 同步 CSS 过渡(括入区淡入)。
单击/双击判定:单击定时 240ms 内来第二次 click → 视为双击(清定时器,invoke open_search);单击定时到期 → toggle 展开。`pointermove` 拖动:移动>4px 后由原生 drag-region 拖动接管(注意:原生拖动与单击冲突用现有 `aurora-drag-hint` 模式处理,按钮区显式 `data-tauri-drag-region="false"`)。

- [ ] **Step 2: 几何记忆+主面板跟随事件**

`onMoved` 监听(tauri://move)→ 防抖 600ms → `invoke("island_save_geometry", {x,y})`(innerPosition 逻辑像素)+ `emit("island-geometry", {x,y,w,h})`(展开态宽 648 时 w 也同步,主面板只关心水平中心,取 (x+w/2))。

- [ ] **Step 3: IslandDock.vue(岛内 Dock)**

从 Dock.vue 移植:条目=34px 瓦片(真实图标 dock_get_icon)/运行绿点(2s 轮询 dock_get_running)/悬停 ✕ 删除徽章(dock_set_items 移除该条)/点击启动(dock_launch + 本地 launching 防连点 900ms + 脉冲 class + 通知父组件收回岛)/＋ 虚线按钮(title 提示"拖拽应用进岛添加")/溢出:条目>9 个 → 渲染前 9+「…」按钮,点击弹绝对定位浮层列全条目(浮层内同样可点启动/✕)。空态:显示"拖拽应用到这里"提示文本。
**图标预热(设计文档 §6 提效要求)**:island 窗口启动即显示 → IslandDock 随 Island.vue 挂载即后台发起全部条目 dock_get_icon(现有后端 LNK_TARGET_CACHE+磁盘缓存保证幂等),用户首次展开岛时图标已就绪、零延迟。
拖放入岛(Island.vue 内 onDragDropEvent):payload 文件路径,`.exe`/`.lnk` → `dock_set_items` 追加(dock_add 现有逻辑复用,去重)并提示;非 exe/lnk 忽略+提示。

- [ ] **Step 4: 删除 Dock.vue 与旧引用**

grep `Dock.vue`/`<Dock` 引用(SearchBar.vue 内)——SearchBar 将在 Task 4 退役,若 Task 3 先行删除 Dock.vue 会导致 pnpm build 失败:改为 Task 3 保留 Dock.vue 不动,**把 SearchBar.vue 内 `<Dock v-if="enableDock" />` 行删除**(SearchBar 退役前的中间态,主面板不再有 Dock)。build 验证。

- [ ] **Step 5: 验证+commit**

`pnpm build` 通过。commit: "Phase6 灵动岛:药丸三态+Dock 括入+拖放入岛+几何记忆"

---

### Task 4: 主面板前端(五视图合一+打字即搜+位置跟随)

**Files:**
- Create: `src/components/core/MainPanel.vue`(主面板壳:header/视图切换/打字即搜/Esc 两级/位置跟随/footer)
- Create: `src/components/core/views/SmallDesktopView.vue`(DrawerPanel 内容迁移,根容器 .aurora-panel 由壳统一)
- Create: `src/components/core/views/ClipboardView.vue`(ClipboardPanel 内容迁移)
- Create: `src/components/core/views/AIView.vue`(AIPanel 内容迁移)
- Create: `src/components/core/views/SearchView.vue`(SearchBar 结果列表逻辑迁移:应用/文件双组+最近打开+图标)
- Modify: `src/main.ts`(search label → MainPanel)
- Modify: `src/App.vue`(删 drawer/clipboard/ai_panel 分支)
- 退役(删除):`src/components/core/SearchBar.vue`、`src/components/core/ClipboardPanel.vue`、`src/components/core/AIPanel.vue`、`src/components/FileDrawer/DrawerPanel.vue`(内容迁移后删除,DrawerPanel 依赖的 FileItem.vue 保留)
- Modify: `src/components/core/Settings.vue`(作为视图嵌入:去掉自身窗口级 footer/把手,保留内容)

**Interfaces:**
- Consumes: Task 1 的窗口(search 680×520)+事件 panel-open-view;Task 2 的 .aurora-panel/AuroraIcon;Task 3 的 island-geometry 事件;现有全部数据命令(config_load/search_apps/search_files/open_item/dock_get_icon/clipboard_*/ai_*/drawer_*)
- Produces: 主面板完整交互——默认视图 small-desktop;打字即搜;Esc 两级(搜索态→small-desktop→关窗);位置跟随岛+面板可拖但位置不落盘;onResized 防抖 search_save_geometry(尺寸记忆)
- 视图 id 契约: `"small-desktop"|"search"|"clipboard"|"ai"|"settings"`(后端 panel-open-view 事件 view 值仅 drawer/clipboard/ai 三种,前端映射 drawer→small-desktop)

- [ ] **Step 1: MainPanel.vue 壳**

结构(参照设计稿 .main-panel):
```
.aurora-panel 根(整窗拖动 drag-region=true,滚动区/控件 false)
├─ header: head-left(标题 span #headTitle / 输入框 input #q 二态显示) + view-switch(5 个 AuroraIcon 按钮)
├─ view-body: KeepAlive + v-if 当前视图(5 组件)
└─ footer: 快捷键 keys + 拖拽把手(AuroraIcon grip)
```
视图切换 `showView(id)`:search → 显示输入框聚焦;其余 → 显示标题。KeepAlive 缓存 4 个视图(small-desktop/clipboard/ai/settings),search 不缓存。
打字即搜:window keydown——面板可见且 `e.key.length===1` 且无修饰键且 `!e.isComposing` 且当前非 search → showView("search") 并把字符写入输入框。
Esc 两级:search 且有输入 → 清空回 small-desktop;search 无输入或非 search → `getCurrentWindow().hide()`。
事件 `panel-open-view` 监听 → 映射视图切换(仅面板呼出后有效,事件在 show 后到,天然时序正确)。
位置跟随:监听 `island-geometry` → 面板可见时 `getCurrentWindow().setPosition`(水平中心对齐岛中心,垂直岛底+12;clamp 视口)。
尺寸记忆:onResized 防抖 600ms → `invoke("search_save_geometry", {x:0,y:0,w,h})`(x/y 传 0 占位,后端 setup 已不恢复位置)。
呼出重置:监听 `tauri://show` → 恢复默认视图 small-desktop(除非本次是热键带视图)。

- [ ] **Step 2: 视图迁移**

SmallDesktopView=DrawerPanel 内容(分类 tab+网格+watcher 逻辑原样,去掉窗口级 header/footer/drag-region);ClipboardView/AIView 同理;SearchView=SearchBar 的结果区逻辑(150ms 防抖/seq 防过期/应用+文件双组/最近打开/图标缓存/dock_get_icon 回退),输入框由壳 header 提供,通过 props/emit 与壳联动(简单起见:输入状态由壳持有,SearchView 接收 query prop+结果渲染+选中态,emit open)。
Settings.vue 作为 settings 视图直接嵌入(现有 KeepAlive 保留;删除其窗口级把手)。

- [ ] **Step 3: main.ts / App.vue 分流收敛**

main.ts:`label === "search" ? MainPanel : label === "island" ? Island : App`;App.vue 删 drawer/clipboard/ai_panel 三个分支(只留 wallpaper 与兜底)。删除退役组件文件。grep 确认无残留引用。

- [ ] **Step 4: 验证+commit**

`pnpm build` 通过(无未引用错误)。commit: "Phase6 主面板:五视图合一+打字即搜+位置跟随岛"

---

### Task 5: 设置页皮肤区块+emoji 清剿

**Files:**
- Modify: `src/components/core/Settings.vue`(主题区块改造+emoji 替换)
- Modify: `src/stores/config.ts`(AppConfig 类型加 skin;save 载荷含 skin)

**Interfaces:**
- Consumes: AuroraIcon;apply_theme(skin);config_load/config_save
- Produces: 主题区块=皮肤 4 胶囊(深空极光/午夜/拂晓/翠野)+强调色 4 圆点(新色值);保存后热生效(apply_theme + aurora:config-saved 广播沿用)

- [ ] **Step 1: stores/config.ts 类型**

AppConfig 接口加 `skin: string`(前端镜像后端字段);默认值兜底 "deep"。

- [ ] **Step 2: Settings 主题区块改造**

原「外观三胶囊(浅色/深色/跟随系统)+强调色 4 圆点」区块改为:皮肤 4 胶囊(chip 组,data-skin 值 deep/midnight/dawn/verdant)+强调色 4 圆点(色值 #38bdf8/#a78bfa/#34d399/#fbbf24,token 名不变 blue/purple/green/orange)。保存后调用 `apply_theme({theme_mode, theme_accent, skin})`(theme_mode 保持写配置兼容)。区块标题用 Task 2 的 accent 竖条样式(已有)。

- [ ] **Step 3: Settings 内 emoji 清剿**

grep Settings.vue 剩余 emoji(⠿/✕ 等)→ 替换 AuroraIcon(grip/close)。同时 grep 全 src 残留 emoji(Island/SearchView/视图组件),除空态装饰(✦ 类)外全部替换。

- [ ] **Step 4: 验证+commit**

`pnpm build` 通过。commit: "Phase6 设置页:皮肤区块+强调色新色值+emoji 清剿"

---

### Task 6: 集成收尾(质量门+内存+文档)

**Files:**
- Modify: `docs/开发进度.md`(Phase6 记录+下一步行动)
- Modify: `README.md`/`README.zh-CN.md`(新形态描述:一岛一窗/皮肤/交互)
- Modify: `docs/真机验收总清单.md`(Phase6 验收项替换旧窗口项)

- [ ] **Step 1: 全量质量门**

`cargo test`(预期 325+2=327 全绿;若 win_utils 等测试受影响需更新)、`pnpm build`、`pnpm tauri build` 全绿;构建 0 警告。

- [ ] **Step 2: release 内存复测**

`src-tauri/target/release/aurora-desktop.exe` 启动后 10s/20s/30s 三采样 WS/PRIV(Get-Process aurora-desktop),断言 <120MB,与基线(WS 50.8/PRIV 21.2)不显著劣化;确认托盘/热键/岛/面板基本链路可用。

- [ ] **Step 3: 文档同步**

开发进度.md:Phase6 行+任务看板+下一步行动(真机验收清单);README 两版:形态描述/快捷键语义(抽屉等热键=呼出主面板对应视图)/皮肤卖点;真机验收总清单:Phase6 验收项(设计文档 §9 七条)置顶。

- [ ] **Step 4: 最终 commit+push**

commit: "Phase6 收尾:质量门全绿+内存达标+文档同步";push origin main(直连,失败走 `-c http.proxy=` 重试)。
